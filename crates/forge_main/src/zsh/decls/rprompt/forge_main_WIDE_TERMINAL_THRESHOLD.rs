use std :: fmt :: { self , Display } ; use convert_case :: { Case , Casing } ; use derive_setters :: Setters ; use forge_config :: ForgeConfig ; use forge_domain :: { AgentId , Effort , ModelId , TokenCount } ; use super :: style :: { ZshColor , ZshStyle } ; use crate :: utils :: humanize_number ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Terminal width (in columns) at which the reasoning effort label switches"] # [doc = " from the compact three-letter form to the full uppercase label."] # [doc = ""] # [doc = " Widths greater than or equal to this threshold render the full label"] # [doc = " (e.g. `MEDIUM`, `HIGH`); widths below it collapse to the first three"] # [doc = " characters (e.g. `MED`, `HIG`). The value is intentionally a coarse"] # [doc = " static threshold — typical RPROMPT content is around 40-50 visible"] # [doc = " cells, so 100 columns leaves enough room on the left for most LPROMPTs"] # [doc = " and comfortable typing space once the full label is shown."] const WIDE_TERMINAL_THRESHOLD : usize = 100 ;