use std :: fmt :: { self , Display } ; use convert_case :: { Case , Casing } ; use derive_setters :: Setters ; use forge_config :: ForgeConfig ; use forge_domain :: { AgentId , Effort , ModelId , TokenCount } ; use super :: style :: { ZshColor , ZshStyle } ; use crate :: utils :: humanize_number ; use pretty_assertions :: assert_eq ; use super :: * ; impl ZshRPrompt { # [doc = " Constructs a [`ZshRPrompt`] with currency settings populated from the"] # [doc = " provided [`ForgeConfig`]."] pub fn from_config (config : & ForgeConfig) -> Self { Self :: default () . currency_symbol (config . currency_symbol . clone ()) . conversion_ratio (config . currency_conversion_rate . value ()) } }