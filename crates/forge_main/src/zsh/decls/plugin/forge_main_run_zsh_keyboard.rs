use std :: fs ; use std :: io :: { BufRead , BufReader } ; use std :: path :: PathBuf ; use std :: process :: Stdio ; use anyhow :: { Context , Result } ; use clap :: CommandFactory ; use clap_complete :: generate ; use clap_complete :: shells :: Zsh ; use include_dir :: { Dir , include_dir } ; use crate :: cli :: Cli ; use std :: io :: Write ; use std :: sync :: { LazyLock , Mutex } ; use super :: * ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use tempfile :: TempDir ; use tempfile :: TempDir ; use tempfile :: TempDir ; use tempfile :: TempDir ; use tempfile :: TempDir ; # [doc = " Shows ZSH keyboard shortcuts with streaming output"] # [doc = ""] # [doc = " # Errors"] # [doc = ""] # [doc = " Returns error if the keyboard script cannot be executed"] pub fn run_zsh_keyboard () -> Result < () > { let script_content = include_str ! ("../../../../shell-plugin/keyboard.zsh") ; execute_zsh_script_with_streaming (script_content , "keyboard") }