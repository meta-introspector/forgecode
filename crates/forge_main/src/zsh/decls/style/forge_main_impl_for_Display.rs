use std :: fmt :: { self , Display } ; use super :: * ; impl Display for ZshStyled < '_ > { fn fmt (& self , f : & mut fmt :: Formatter < '_ >) -> fmt :: Result { if self . bold { write ! (f , "%B") ? ; } if let Some (ref color) = self . fg { write ! (f , "%F{{{}}}" , color) ? ; } write ! (f , "{}" , self . text) ? ; if self . fg . is_some () { write ! (f , "%f") ? ; } if self . bold { write ! (f , "%b") ? ; } Ok (()) } }