use std :: path :: { Path , PathBuf } ; use std :: sync :: Arc ; use forge_select :: { ForgeWidget , PreviewLayout , PreviewPlacement , SelectRow } ; use forge_walker :: Walker ; use reedline :: { Completer , Span , Suggestion } ; use crate :: completer :: CommandCompleter ; use crate :: completer :: search_term :: SearchTerm ; use crate :: model :: ForgeCommandManager ; pub fn select_workspace_file (cwd : & Path , query : Option < String >) -> anyhow :: Result < Option < String > > { let files : Vec < String > = Walker :: max_all () . cwd (cwd . to_path_buf ()) . get_blocking () . unwrap_or_default () . into_iter () . map (| file | file . path) . collect () ; if files . is_empty () { return Ok (None) ; } let has_bat = std :: process :: Command :: new ("bat") . arg ("--version") . stdout (std :: process :: Stdio :: null ()) . stderr (std :: process :: Stdio :: null ()) . status () . is_ok () ; let cat_cmd = if has_bat { "bat --color=always --style=numbers,changes --line-range=:500" } else { "cat" } ; let preview_cmd = format ! ("if [ -d {{}} ]; then ls -la --color=always {{}} 2>/dev/null || ls -la {{}}; else {cat_cmd} {{}}; fi") ; let rows : Vec < SelectRow > = files . into_iter () . map (| path | SelectRow { raw : path . clone () , display : path . clone () , search : path . clone () , fields : vec ! [path] , }) . collect () ; Ok (ForgeWidget :: select_rows ("File ❯ " , rows) . query (Some (query . unwrap_or_default ())) . preview (Some (preview_cmd)) . preview_layout (PreviewLayout { placement : PreviewPlacement :: Bottom , percent : 75 }) . prompt () ? . map (| row | row . raw)) }