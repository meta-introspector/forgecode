use nu_ansi_term :: { Color , Style } ; use reedline :: { Highlighter , StyledText } ; use super :: * ; # [doc = " Syntax highlighter for the forge readline prompt."] # [doc = ""] # [doc = " Applies visual styles to recognised input patterns as the user types:"] # [doc = " - Commands (`:foo` or `/foo` for backward compatibility) are rendered in"] # [doc = "   yellow bold."] # [doc = " - File mentions (`@[path]`) are rendered in cyan bold."] # [doc = " - Shell pass-through commands (`!cmd`) are rendered in magenta."] # [doc = " - All other text is rendered in the default terminal style."] pub struct ForgeHighlighter ;