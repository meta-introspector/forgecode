use std :: sync :: { Arc , Mutex } ; use clap :: error :: ErrorKind ; use clap :: { Parser , Subcommand } ; use forge_api :: { AgentInfo , Model , Template } ; use forge_domain :: UserCommand ; use strum :: { EnumProperty , IntoEnumIterator } ; use strum_macros :: { EnumIter , EnumProperty } ; use crate :: info :: Info ; use std :: fmt :: Display ; use colored :: Colorize ; use console :: strip_ansi_codes ; use forge_api :: { AnyProvider , InputModality , Model , ModelId , ModelSource , ProviderId , ProviderResponse , } ; use forge_domain :: Provider ; use pretty_assertions :: assert_eq ; use url :: Url ; use super :: * ; use crate :: display_constants :: markers ; # [doc = " Top-level Clap parser used to dispatch slash/colon commands."] # [doc = ""] # [doc = " The sentinel character (`/` or `:`) is stripped before passing tokens here,"] # [doc = " so Clap only sees the subcommand name and its arguments."] # [derive (Debug , Parser)] # [command (name = "forge_cmd" , no_binary_name = true , disable_help_subcommand = true)] struct ClapCmd { # [command (subcommand)] sub : AppCommand , }