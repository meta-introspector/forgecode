use std :: collections :: HashMap ; use std :: fmt ; use indexmap :: IndexSet ; use crate :: display_constants :: headers ; use crate :: info :: { Info , Section } ; use convert_case :: Casing ; use pretty_assertions :: assert_eq ; use super :: * ; use convert_case :: Case ; use convert_case :: Case ; # [doc = " Porcelain is an intermediate representation that converts Info into a flat,"] # [doc = " tabular structure suitable for machine-readable output."] # [doc = ""] # [doc = " Structure: Vec<(String, Vec<Option<String>>)>"] # [doc = " - First element: Section name"] # [doc = " - Second element: Vec of Option<String> pairs where:"] # [doc = "   - Index 0, 2, 4... are keys"] # [doc = "   - Index 1, 3, 5... are values"] # [doc = "   - None = missing value"] # [derive (Debug , PartialEq , Clone)] pub struct Porcelain (Vec < Vec < Option < String > > >) ;