use std :: collections :: HashMap ; use std :: net :: { IpAddr , SocketAddr , TcpListener } ; use std :: sync :: Arc ; use std :: sync :: atomic :: { AtomicBool , Ordering } ; use std :: time :: { Duration , Instant } ; use forge_domain :: CodeRequest ; use tiny_http :: { Header , Method , Request , Response , Server , StatusCode } ; use url :: { Host , Url } ; use std :: io :: { Read , Write } ; use std :: net :: { Shutdown , SocketAddr , TcpListener , TcpStream } ; use std :: sync :: Arc ; use std :: sync :: atomic :: AtomicBool ; use std :: thread ; use forge_domain :: { OAuthConfig , PkceVerifier , State } ; use pretty_assertions :: assert_eq ; use super :: * ; fn localhost_oauth_redirect_uri (request : & CodeRequest) -> Option < Url > { request . oauth_config . redirect_uri . as_ref () . and_then (| uri | Url :: parse (uri) . ok ()) . filter (| uri | { uri . scheme () == "http" && uri . port () . is_some () && (matches ! (uri . host () , Some (Host :: Domain ("localhost"))) || uri . host () . is_some_and (| host | match host { Host :: Ipv4 (ip) => ip . is_loopback () , Host :: Ipv6 (ip) => ip . is_loopback () , Host :: Domain (_) => false , })) }) }