use forge_domain :: SyncProgress ; use forge_api :: WorkspaceId ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Extensions for formatting `SyncProgress` events as human-readable strings."] # [doc = ""] # [doc = " This module contains display logic for sync operation events, converting"] # [doc = " them into user-friendly messages for the UI layer."] pub trait SyncProgressDisplay { # [doc = " Returns a human-readable status message for this event."] # [doc = ""] # [doc = " Returns `None` for internal events that don't need user-facing messages."] fn message (& self) -> Option < String > ; }