use std :: process :: Command ; use std :: env ; use std :: ffi :: OsString ; use std :: sync :: { LazyLock , Mutex , MutexGuard } ; use super :: * ; # [doc = " Returns true if we should install the extension"] # [doc = ""] # [doc = " This will return true only when:"] # [doc = " - Not running on native Windows"] # [doc = " - Running in VS Code terminal"] # [doc = " - Extension is not installed"] pub fn should_install_extension () -> bool { if cfg ! (windows) { return false ; } is_vscode_terminal () && ! is_extension_installed () }