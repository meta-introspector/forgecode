use forge_tracker :: { EventKind , ToolCallPayload } ; use crate :: TRACKER ; # [doc = " Dispatches an event blockingly"] # [doc = " This is useful for events that are not expected to be dispatched in the"] # [doc = " background"] fn dispatch_blocking (event : EventKind) { tokio :: task :: block_in_place (| | { tokio :: runtime :: Handle :: current () . block_on (TRACKER . dispatch (event)) }) . ok () ; }