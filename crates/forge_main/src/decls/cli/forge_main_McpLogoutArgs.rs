use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [derive (Parser , Debug , Clone)] pub struct McpLogoutArgs { # [doc = " Name of the MCP server to remove credentials for, or \"all\" to"] # [doc = " remove all MCP OAuth credentials."] pub name : String , }