use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [doc = " Shell extension commands."] # [derive (Subcommand , Debug , Clone)] pub enum ZshCommandGroup { # [doc = " Generate shell plugin script"] Plugin , # [doc = " Generate shell theme"] Theme , # [doc = " Run diagnostics on shell environment"] Doctor , # [doc = " Get rprompt information (model and conversation stats) for shell"] # [doc = " integration."] Rprompt , # [doc = " Setup zsh integration by updating .zshrc with plugin and theme"] Setup , # [doc = " Show keyboard shortcuts for ZSH line editor"] Keyboard , # [doc = " Format buffer text by wrapping file paths in @[...] syntax."] # [doc = ""] # [doc = " Used by the zsh plugin to delegate path detection and wrapping to"] # [doc = " Rust where the logic is well-tested across all terminal environments."] Format { # [doc = " The text buffer to format."] # [arg (long)] buffer : String , } , }