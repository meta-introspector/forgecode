use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [doc = " Group of Commit-related commands"] # [derive (Parser , Debug , Clone)] pub struct CommitCommandGroup { # [doc = " Preview the commit message without committing"] # [arg (long)] pub preview : bool , # [doc = " Maximum git diff size in bytes (default: 100k)"] # [doc = ""] # [doc = " Limits the size of the git diff sent to the AI model. Large diffs are"] # [doc = " truncated to save tokens and reduce API costs. Minimum value is 5000"] # [doc = " bytes."] # [arg (long = "max-diff" , default_value = "100000" , value_parser = clap :: builder :: RangedI64ValueParser ::< usize >:: new () . range (5000 ..))] pub max_diff_size : Option < usize > , # [doc = " Git diff content (used internally for piped input)"] # [doc = ""] # [doc = " This field is populated when diff content is piped to the commit"] # [doc = " command. Users typically don't set this directly; instead, they pipe"] # [doc = " diff content: `git diff | forge commit --preview`"] # [arg (skip)] pub diff : Option < String > , # [doc = " Additional text to customize the commit message"] # [doc = ""] # [doc = " Provide additional context or instructions for the AI to use when"] # [doc = " generating the commit message. Multiple words can be provided without"] # [doc = " quotes: `forge commit fix typo in readme`"] pub text : Vec < String > , }