use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [derive (Subcommand , Debug , Clone)] pub enum ProviderCommand { # [doc = " Authenticate with an API provider."] # [doc = ""] # [doc = " Shows an interactive menu when no provider name is specified."] Login { # [doc = " Provider name to authenticate with."] provider : Option < ProviderId > , } , # [doc = " Remove provider credentials."] # [doc = ""] # [doc = " Shows an interactive menu when no provider name is specified."] Logout { # [doc = " Provider name to log out from."] provider : Option < ProviderId > , } , # [doc = " List available providers."] List { # [doc = " Filter providers by type (e.g., llm, context_engine). Can be"] # [doc = " specified multiple times."] # [arg (long = "type" , short = 't')] types : Vec < forge_domain :: ProviderType > , } , }