use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [doc = " Command group for the `forge select` interactive picker."] # [doc = ""] # [doc = " Subcommands provide purpose-built pickers for specific domain types (models,"] # [doc = " agents, providers, etc.) that fetch data internally and output the selected"] # [doc = " value."] # [derive (Parser , Debug , Clone)] pub struct SelectCommandGroup { # [command (subcommand)] pub command : SelectCommand , }