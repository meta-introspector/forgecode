use std :: path :: PathBuf ; use clap :: { Parser , Subcommand , ValueEnum } ; use forge_domain :: { AgentId , ConversationId , Effort , ModelId , ProviderId } ; use clap :: Parser ; use pretty_assertions :: assert_eq ; use super :: * ; use std :: path :: PathBuf ; # [derive (Subcommand , Debug , Clone)] pub enum WorkspaceCommand { # [doc = " Synchronize a directory for semantic search."] Sync { # [doc = " Path to the directory to sync"] # [arg (default_value = ".")] path : PathBuf , # [doc = " Automatically initialize the workspace before syncing if it has not"] # [doc = " been initialized yet."] # [arg (long)] init : bool , } , # [doc = " List all workspaces."] List { # [doc = " Output in machine-readable format"] # [arg (short , long)] porcelain : bool , } , # [doc = " Query the workspace."] Query { # [doc = " Search query."] query : String , # [doc = " Path to the directory to index (used when no subcommand is"] # [doc = " provided)."] # [arg (default_value = ".")] path : PathBuf , # [doc = " Maximum number of results to return."] # [arg (short , long , default_value = "10")] limit : usize , # [doc = " Number of highest probability tokens to consider (1-1000)."] # [arg (long)] top_k : Option < u32 > , # [doc = " Describe your intent or goal to filter results for relevance."] # [arg (long , short = 'r')] use_case : String , # [doc = " Filter results to files starting with this prefix."] # [arg (long)] starts_with : Option < String > , # [doc = " Filter results to files ending with this suffix."] # [arg (long)] ends_with : Option < String > , } , # [doc = " Show workspace information for an indexed directory."] Info { # [doc = " Path to the directory to get information for"] # [arg (default_value = ".")] path : PathBuf , } , # [doc = " Delete one or more workspaces."] Delete { # [doc = " Workspace IDs to delete"] workspace_ids : Vec < String > , } , # [doc = " Show sync status of all files in the workspace."] Status { # [doc = " Path to the directory to check status for"] # [arg (default_value = ".")] path : PathBuf , # [doc = " Output in machine-readable format"] # [arg (short , long)] porcelain : bool , } , # [doc = " Initialize an empty workspace for the provided directory"] Init { # [doc = " Path to the directory to initialize as a workspace"] # [arg (default_value = ".")] path : PathBuf , # [doc = " Automatically confirm initialization without prompting"] # [arg (short = 'y' , long)] yes : bool , } , }