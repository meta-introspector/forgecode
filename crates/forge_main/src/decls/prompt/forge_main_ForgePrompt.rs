use std :: borrow :: Cow ; use std :: fmt :: Write ; use std :: path :: PathBuf ; use convert_case :: { Case , Casing } ; use derive_setters :: Setters ; use forge_api :: { AgentId , Effort , ModelId , Usage } ; use nu_ansi_term :: { Color , Style } ; use reedline :: { Prompt , PromptHistorySearchStatus } ; use crate :: display_constants :: markers ; use crate :: utils :: humanize_number ; use nu_ansi_term :: Style ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Very Specialized Prompt for the Agent Chat"] # [derive (Clone , Setters)] # [setters (strip_option , borrow_self)] pub struct ForgePrompt { pub cwd : PathBuf , pub usage : Option < Usage > , pub agent_id : AgentId , pub model : Option < ModelId > , # [doc = " Currently configured reasoning effort level for the active model,"] # [doc = " rendered to the right of the model when set. `Effort::None` is"] # [doc = " suppressed (see [`ForgePrompt::render_prompt_right`])."] pub reasoning_effort : Option < Effort > , pub git_branch : Option < String > , }