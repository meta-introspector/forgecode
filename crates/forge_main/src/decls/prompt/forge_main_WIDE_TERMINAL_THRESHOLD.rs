use std :: borrow :: Cow ; use std :: fmt :: Write ; use std :: path :: PathBuf ; use convert_case :: { Case , Casing } ; use derive_setters :: Setters ; use forge_api :: { AgentId , Effort , ModelId , Usage } ; use nu_ansi_term :: { Color , Style } ; use reedline :: { Prompt , PromptHistorySearchStatus } ; use crate :: display_constants :: markers ; use crate :: utils :: humanize_number ; use nu_ansi_term :: Style ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Terminal width at which the reasoning effort label switches from the"] # [doc = " compact three-letter form (e.g. `MED`) to the full uppercase label"] # [doc = " (e.g. `MEDIUM`). Matches [`crate::zsh::rprompt`] so the CLI and zsh"] # [doc = " integration render identically on equivalent terminals."] const WIDE_TERMINAL_THRESHOLD : usize = 100 ;