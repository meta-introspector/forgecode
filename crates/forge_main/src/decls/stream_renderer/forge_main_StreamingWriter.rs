use std :: borrow :: Cow ; use std :: io ; use std :: sync :: { Arc , Mutex } ; use anyhow :: Result ; use bstr :: ByteSlice ; use colored :: Colorize ; use forge_domain :: ConsoleWriter ; use forge_markdown_stream :: StreamdownRenderer ; use forge_spinner :: SpinnerManager ; # [doc = " Streaming markdown writer with automatic spinner management."] # [doc = ""] # [doc = " Coordinates between markdown rendering and spinner visibility:"] # [doc = " - Stops spinner when content is being written"] # [doc = " - Restarts spinner when idle"] pub struct StreamingWriter < P : ConsoleWriter + 'static > { active : Option < ActiveRenderer < P > > , spinner : SharedSpinner < P > , printer : Arc < P > , }