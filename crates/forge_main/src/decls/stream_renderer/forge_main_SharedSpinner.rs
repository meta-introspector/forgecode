use std :: borrow :: Cow ; use std :: io ; use std :: sync :: { Arc , Mutex } ; use anyhow :: Result ; use bstr :: ByteSlice ; use colored :: Colorize ; use forge_domain :: ConsoleWriter ; use forge_markdown_stream :: StreamdownRenderer ; use forge_spinner :: SpinnerManager ; # [doc = " Shared spinner wrapper that encapsulates locking for thread-safe spinner"] # [doc = " operations."] # [doc = ""] # [doc = " Provides the same API as `SpinnerManager` but handles mutex locking"] # [doc = " internally, releasing the lock immediately after each operation completes."] pub struct SharedSpinner < P : ConsoleWriter > (Arc < Mutex < SpinnerManager < P > > >) ;