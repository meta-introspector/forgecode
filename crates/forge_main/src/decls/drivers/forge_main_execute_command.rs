use anyhow :: Result ; use clap :: Parser ; use forge_domain :: { ConversationId , Effort , ModelId , ProviderId } ; use forge_main :: { Cli , TopLevelCommand , UI } ; use forge_api :: ForgeAPI ; use forge_config :: ForgeConfig ; use forge_main :: cli :: CommitCommandGroup ; use forge_main :: cli :: { ConfigCommand , ConfigCommandGroup , ConfigSetField } ; use forge_main :: cli :: { WorkspaceCommand , WorkspaceCommandGroup } ; # [doc = " Execute a specific forge command programmatically"] # [doc = " "] # [doc = " Example usage:"] # [doc = " ```rust"] # [doc = " let mut driver = ForgeDriver::new().await?;"] # [doc = " driver.commit(true, None, None, vec![\"fix\".to_string(), \"bug\".to_string()]).await?;"] # [doc = " ```"] pub async fn execute_command (command : TopLevelCommand) -> Result < () > { let mut driver = ForgeDriver :: new () . await ? ; driver . execute_command (command) . await }