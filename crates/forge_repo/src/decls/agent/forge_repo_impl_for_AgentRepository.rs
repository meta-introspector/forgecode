use std :: sync :: Arc ; use anyhow :: { Context , Result } ; use forge_app :: { AgentRepository , DirectoryReaderInfra , EnvironmentInfra , FileInfoInfra } ; use forge_config :: ForgeConfig ; use forge_domain :: { ModelId , ProviderId , Template , ToolName } ; use gray_matter :: Matter ; use gray_matter :: engine :: YAML ; use crate :: agent_definition :: AgentDefinition ; use std :: collections :: HashMap ; use forge_domain :: AgentId ; use insta :: { assert_snapshot , assert_yaml_snapshot } ; use pretty_assertions :: assert_eq ; use super :: * ; # [async_trait :: async_trait] impl < F : FileInfoInfra + EnvironmentInfra < Config = ForgeConfig > + DirectoryReaderInfra > AgentRepository for ForgeAgentRepository < F > { async fn get_agents (& self) -> anyhow :: Result < Vec < forge_domain :: Agent > > { let config = self . infra . get_config () ? ; let agent_defs = self . load_agents () . await ? ; let session = config . session . clone () . ok_or (forge_domain :: Error :: NoDefaultSession) ? ; Ok (agent_defs . into_iter () . map (| def | { def . into_agent (ProviderId :: from (session . provider_id . clone ()) , ModelId :: from (session . model_id . clone ()) ,) }) . collect ()) } async fn get_agent_infos (& self) -> anyhow :: Result < Vec < forge_domain :: AgentInfo > > { let agent_defs = self . load_agents () . await ? ; Ok (agent_defs . into_iter () . map (| def | forge_domain :: AgentInfo { id : def . id , title : def . title , description : def . description , }) . collect ()) } }