use std :: sync :: Arc ; use anyhow :: { Context , Result } ; use forge_app :: { AgentRepository , DirectoryReaderInfra , EnvironmentInfra , FileInfoInfra } ; use forge_config :: ForgeConfig ; use forge_domain :: { ModelId , ProviderId , Template , ToolName } ; use gray_matter :: Matter ; use gray_matter :: engine :: YAML ; use crate :: agent_definition :: AgentDefinition ; use std :: collections :: HashMap ; use forge_domain :: AgentId ; use insta :: { assert_snapshot , assert_yaml_snapshot } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Infrastructure implementation for loading agent definitions from multiple"] # [doc = " sources:"] # [doc = " 1. Built-in agents (embedded in the application)"] # [doc = " 2. Global custom agents (from ~/.forge/agents/ directory)"] # [doc = " 3. Project-local agents (from .forge/agents/ directory in current working"] # [doc = "    directory)"] # [doc = ""] # [doc = " ## Agent Precedence"] # [doc = " When agents have duplicate IDs across different sources, the precedence"] # [doc = " order is: **CWD (project-local) > Global custom > Built-in**"] # [doc = ""] # [doc = " This means project-local agents can override global agents, and both can"] # [doc = " override built-in agents."] # [doc = ""] # [doc = " ## Directory Resolution"] # [doc = " - **Built-in agents**: Embedded in application binary"] # [doc = " - **Global agents**: `~/forge/agents/*.md`"] # [doc = " - **CWD agents**: `./.forge/agents/*.md` (relative to current working"] # [doc = "   directory)"] # [doc = ""] # [doc = " Missing directories are handled gracefully and don't prevent loading from"] # [doc = " other sources."] pub struct ForgeAgentRepository < I > { infra : Arc < I > , }