use async_openai :: types :: responses :: { self as oai , CreateResponse } ; use forge_domain :: Transformer ; use async_openai :: types :: responses as oai ; use forge_app :: domain :: ContextMessage ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: provider :: FromDomain ; # [doc = " Transformer that adjusts Responses API requests for the Codex backend."] # [doc = ""] # [doc = " The Codex backend at `chatgpt.com/backend-api/codex/responses` differs from"] # [doc = " the standard OpenAI Responses API in several ways:"] # [doc = " - `store` **must** be `false` (the server defaults to `true` and rejects"] # [doc = "   omitted values)."] # [doc = " - `temperature` is not supported and must be stripped."] # [doc = " - `max_output_tokens` is not supported and must be stripped."] # [doc = " - `include` always contains `reasoning.encrypted_content` for stateless"] # [doc = "   reasoning continuity."] # [doc = " - `reasoning.effort` and `reasoning.summary` are passed through as-is from"] # [doc = "   the caller."] pub struct CodexTransformer ;