use forge_app :: domain :: Error as DomainError ; use forge_app :: dto :: anthropic :: Error as AnthropicError ; use forge_app :: dto :: openai :: { Error , ErrorCode , ErrorResponse } ; use forge_config :: RetryConfig ; use anyhow :: anyhow ; use forge_app :: dto :: openai :: { Error , ErrorCode , ErrorResponse } ; use super :: * ; use tokio :: net :: TcpListener ; # [doc = " Checks if the error is an Anthropic `overloaded_error`, which arrives as an"] # [doc = " SSE event payload rather than an HTTP status code and must be retried."] fn is_anthropic_overloaded_error (error : & anyhow :: Error) -> bool { error . downcast_ref :: < AnthropicError > () . is_some_and (| e | matches ! (e , AnthropicError :: OverloadedError { .. })) }