use std :: sync :: Arc ; use anyhow :: Result ; use forge_app :: domain :: { ChatCompletionMessage , Context as ChatContext , Model , ModelId , Provider , ProviderResponse , ResultStream , } ; use forge_app :: { EnvironmentInfra , HttpInfra } ; use forge_domain :: ChatRepository ; use url :: Url ; use crate :: provider :: anthropic :: AnthropicResponseRepository ; use crate :: provider :: google :: GoogleResponseRepository ; use crate :: provider :: openai :: OpenAIResponseRepository ; use crate :: provider :: openai_responses :: OpenAIResponsesResponseRepository ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " OpenCode provider that routes to different backends based on model:"] # [doc = " - Claude models (claude-*) -> Anthropic endpoint"] # [doc = " - GPT-5 models (gpt-5*) -> OpenAIResponses endpoint"] # [doc = " - Gemini models (gemini-*) -> Google endpoint"] # [doc = " - Others (GLM, MiniMax, Kimi, etc.) -> OpenAI endpoint"] # [doc = ""] # [doc = " Supports both OpenCode Zen and OpenCode Go by deriving endpoint URLs"] # [doc = " from the provider's configured base URL rather than hardcoding them."] pub struct OpenCodeZenResponseRepository < F > { openai_repo : OpenAIResponseRepository < F > , codex_repo : OpenAIResponsesResponseRepository < F > , anthropic_repo : AnthropicResponseRepository < F > , google_repo : GoogleResponseRepository < F > , }