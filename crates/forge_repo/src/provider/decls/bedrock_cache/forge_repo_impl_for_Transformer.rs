use aws_sdk_bedrockruntime :: operation :: converse_stream :: ConverseStreamInput ; use aws_sdk_bedrockruntime :: types :: { CachePointBlock , CachePointType , ContentBlock , SystemContentBlock , } ; use forge_domain :: Transformer ; use forge_domain :: { Context , ContextMessage , Role , TextMessage } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: provider :: FromDomain ; impl Transformer for SetCache { type Value = ConverseStreamInput ; # [doc = " Implements a simple two-breakpoint cache strategy:"] # [doc = " 1. Cache after the first system message (if exists)"] # [doc = " 2. Cache after the last message"] fn transform (& mut self , mut request : Self :: Value) -> Self :: Value { let sys_len = request . system . as_ref () . map_or (0 , | msgs | msgs . len ()) ; let msg_len = request . messages . as_ref () . map_or (0 , | msgs | msgs . len ()) ; if sys_len == 0 && msg_len == 0 { return request ; } if let Some (system_messages) = request . system . as_mut () && ! system_messages . is_empty () { system_messages . insert (1 , SystemContentBlock :: CachePoint (CachePointBlock :: builder () . r#type (CachePointType :: Default) . build () . expect ("Failed to build CachePointBlock") ,) ,) ; } if let Some (messages) = request . messages . as_mut () && let Some (last_message) = messages . last_mut () { last_message . content . push (ContentBlock :: CachePoint (CachePointBlock :: builder () . r#type (CachePointType :: Default) . build () . expect ("Failed to build CachePointBlock") ,)) ; } request } }