use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl From < & forge_domain :: ToolValue > for ToolValueRecord { fn from (value : & forge_domain :: ToolValue) -> Self { match value { forge_domain :: ToolValue :: Text (text) => Self :: Text (text . clone ()) , forge_domain :: ToolValue :: AI { value , conversation_id } => Self :: AI { value : value . clone () , conversation_id : conversation_id . into_string () , } , forge_domain :: ToolValue :: Image (img) => Self :: Image (ImageRecord :: from (img)) , forge_domain :: ToolValue :: Empty => Self :: Empty , } } }