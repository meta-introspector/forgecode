use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl TryFrom < ToolResultRecord > for forge_domain :: ToolResult { type Error = anyhow :: Error ; fn try_from (record : ToolResultRecord) -> anyhow :: Result < Self > { Ok (forge_domain :: ToolResult { name : record . name . into () , call_id : record . call_id . map (Into :: into) , output : record . output . try_into () ? , }) } }