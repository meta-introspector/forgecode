use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl From < ImageRecord > for forge_domain :: Image { fn from (record : ImageRecord) -> Self { forge_domain :: Image :: new_base64 (record . url . strip_prefix (& format ! ("data:{};base64," , record . mime_type)) . unwrap_or (& record . url) . to_string () , record . mime_type ,) } }