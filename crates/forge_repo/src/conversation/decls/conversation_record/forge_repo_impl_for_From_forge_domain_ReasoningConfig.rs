use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl From < & forge_domain :: ReasoningConfig > for ReasoningConfigRecord { fn from (config : & forge_domain :: ReasoningConfig) -> Self { Self { effort : config . effort . as_ref () . map (EffortRecord :: from) , max_tokens : config . max_tokens , exclude : config . exclude , enabled : config . enabled , } } }