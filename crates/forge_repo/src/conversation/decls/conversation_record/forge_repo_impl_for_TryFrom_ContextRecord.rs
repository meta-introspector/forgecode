use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl TryFrom < ContextRecord > for Context { type Error = anyhow :: Error ; fn try_from (record : ContextRecord) -> anyhow :: Result < Self > { let conversation_id = record . conversation_id . map (ConversationId :: parse) . transpose () ? ; tracing :: debug ! ("Deserializing context for conversation: {:?}" , conversation_id) ; let messages : Result < Vec < _ > , _ > = record . messages . into_iter () . enumerate () . map (| (idx , v) | { v . try_into () . map_err (| e : anyhow :: Error | { eprintln ! ("Failed to deserialize message {} for conversation {:?}: {}" , idx , conversation_id , e) ; e }) }) . collect () ; let tools : Result < Vec < _ > , _ > = record . tools . into_iter () . map (TryInto :: try_into) . collect () ; Ok (Context { conversation_id , initiator : record . initiator , messages : messages ? , tools : tools ? , tool_choice : record . tool_choice . map (Into :: into) , max_tokens : record . max_tokens , temperature : record . temperature . map (forge_domain :: Temperature :: new_unchecked) , top_p : record . top_p . map (forge_domain :: TopP :: new_unchecked) , top_k : record . top_k . map (forge_domain :: TopK :: new_unchecked) , reasoning : record . reasoning . map (Into :: into) , stream : record . stream , response_format : None , }) } }