use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl From < ReasoningConfigRecord > for forge_domain :: ReasoningConfig { fn from (record : ReasoningConfigRecord) -> Self { forge_domain :: ReasoningConfig { effort : record . effort . map (Into :: into) , max_tokens : record . max_tokens , exclude : record . exclude , enabled : record . enabled , } } }