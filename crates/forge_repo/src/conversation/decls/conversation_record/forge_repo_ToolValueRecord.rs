use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; # [doc = " Repository-specific representation of ToolValue"] # [derive (Debug , Clone , Serialize , Deserialize)] # [serde (rename_all = "camelCase")] pub (super) enum ToolValueRecord { Text (String) , AI { value : String , conversation_id : String , } , Image (ImageRecord) , Empty , # [doc = " Legacy: Markdown-formatted text (now converted to Text)"] Markdown (String) , # [doc = " Legacy: File diff information (now converted to Text summary)"] FileDiff (FileDiffRecord) , # [doc = " Legacy: Paired value for LLM/display (now flattened to first element)"] Pair (Box < ToolValueRecord > , Box < ToolValueRecord >) , }