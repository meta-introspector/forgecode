use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl TryFrom < ToolOutputRecord > for forge_domain :: ToolOutput { type Error = anyhow :: Error ; fn try_from (record : ToolOutputRecord) -> anyhow :: Result < Self > { let values : Result < Vec < _ > , _ > = record . values . into_iter () . map (TryInto :: try_into) . collect () ; Ok (forge_domain :: ToolOutput { is_error : record . is_error , values : values ? }) } }