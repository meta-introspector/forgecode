use anyhow :: Context as _ ; use forge_domain :: { Context , ConversationId } ; use serde :: { Deserialize , Serialize } ; impl TryFrom < ContextMessageRecord > for forge_domain :: MessageEntry { type Error = anyhow :: Error ; fn try_from (record : ContextMessageRecord) -> anyhow :: Result < Self > { Ok (forge_domain :: MessageEntry { message : record . message . try_into () ? , usage : record . usage . map (Into :: into) , }) } }