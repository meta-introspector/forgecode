use std :: collections :: BTreeMap ; use std :: ops :: Deref ; use derive_more :: { Deref , Display , From } ; use derive_setters :: Setters ; use merge :: Merge ; use serde :: { Deserialize , Serialize } ; use strum_macros :: { Display as StrumDisplay , EnumIter , EnumString } ; use serde :: de ; use std :: hash :: { Hash , Hasher } ; use super :: * ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_ne ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use fake :: { Fake , Faker } ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; # [doc = " Persists accepted and rejected MCP config hashes across restarts. A path"] # [doc = " maps to its content hash so that any modification to the file revokes the"] # [doc = " stored decision and triggers a new prompt."] # [derive (Default , Debug , Clone , Serialize , Deserialize)] pub struct McpTrustStore { # [serde (default)] trusted : std :: collections :: HashMap < String , u64 > , # [serde (default)] rejected : std :: collections :: HashMap < String , u64 > , }