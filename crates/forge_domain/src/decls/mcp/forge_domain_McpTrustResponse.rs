use std :: collections :: BTreeMap ; use std :: ops :: Deref ; use derive_more :: { Deref , Display , From } ; use derive_setters :: Setters ; use merge :: Merge ; use serde :: { Deserialize , Serialize } ; use strum_macros :: { Display as StrumDisplay , EnumIter , EnumString } ; use serde :: de ; use std :: hash :: { Hash , Hasher } ; use super :: * ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_ne ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use fake :: { Fake , Faker } ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; # [doc = " The two choices presented to the user when an untrusted project-local"] # [doc = " `.mcp.json` is detected at startup."] # [derive (Debug , Clone , PartialEq , Eq , StrumDisplay , EnumIter , EnumString)] pub enum McpTrustResponse { # [doc = " Allow the servers and remember this decision across future sessions."] # [doc = " The config hash is persisted so the prompt is skipped on next startup"] # [doc = " as long as the file has not changed."] # [strum (to_string = "Accept")] Accept , # [doc = " Reject all servers from this config file."] # [strum (to_string = "Reject")] Reject , }