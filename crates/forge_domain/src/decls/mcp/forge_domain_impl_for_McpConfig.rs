use std :: collections :: BTreeMap ; use std :: ops :: Deref ; use derive_more :: { Deref , Display , From } ; use derive_setters :: Setters ; use merge :: Merge ; use serde :: { Deserialize , Serialize } ; use strum_macros :: { Display as StrumDisplay , EnumIter , EnumString } ; use serde :: de ; use std :: hash :: { Hash , Hasher } ; use super :: * ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_ne ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use fake :: { Fake , Faker } ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; use pretty_assertions :: assert_eq ; impl McpConfig { # [doc = " Compute a deterministic u64 identifier for this config."] # [doc = ""] # [doc = " Uses FNV-64 (a non-cryptographic but stable, seed-free hasher) so the"] # [doc = " same config always produces the same key across process restarts."] # [doc = " This is required for persisted trust-store lookups: `DefaultHasher`"] # [doc = " uses a random seed per-process and would produce a different value on"] # [doc = " every restart, causing \"Trust and remember\" to be ignored."] # [doc = " `BTreeMap` ensures consistent field ordering regardless of insertion"] # [doc = " order."] pub fn cache_key (& self) -> u64 { use std :: hash :: { Hash , Hasher } ; let mut hasher = fnv_rs :: Fnv64 :: default () ; Hash :: hash (self , & mut hasher) ; hasher . finish () } }