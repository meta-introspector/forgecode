use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use crate :: { ModelId , ProviderId } ; # [doc = " Domain-level configuration that pairs a provider with a model."] # [doc = ""] # [doc = " Used as the unified payload for [`super::ConfigOperation`] variants that"] # [doc = " configure a provider/model pair (session, commit, suggest). Both fields are"] # [doc = " required; use `Option<ModelConfig>` at the call-site when the configuration"] # [doc = " is optional."] # [derive (Debug , Clone , Serialize , Deserialize , JsonSchema , PartialEq)] pub struct ModelConfig { # [doc = " The provider ID (e.g. `\"anthropic\"`)."] pub provider : ProviderId , # [doc = " The model ID to use with this provider."] pub model : ModelId , }