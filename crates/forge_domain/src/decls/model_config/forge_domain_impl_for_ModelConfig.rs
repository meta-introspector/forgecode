use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use crate :: { ModelId , ProviderId } ; impl ModelConfig { # [doc = " Creates a new [`ModelConfig`] with the given provider and model."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `provider` - The provider identifier"] # [doc = " * `model` - The model identifier"] pub fn new (provider : impl Into < ProviderId > , model : impl Into < ModelId >) -> Self { Self { provider : provider . into () , model : model . into () } } }