use chrono :: Utc ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; impl < C > Point < C > { # [doc = " Embedding can be created from a part or more of the actual content."] pub fn new (content : C , embedding : Vec < f32 >) -> Self { let now = Utc :: now () ; Self { id : PointId :: generate () , content , embedding , created_at : now , updated_at : now , } } pub fn try_map < D , E > (self , f : impl FnOnce (C) -> std :: result :: Result < D , E > ,) -> std :: result :: Result < Point < D > , E > { Ok (Point { content : f (self . content) ? , id : self . id , embedding : self . embedding , created_at : self . created_at , updated_at : self . updated_at , }) } }