use crate :: { Context , Role } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: MessagePattern ; # [doc = " Strategy for context compaction that unifies different compaction approaches"] # [derive (Debug , Clone)] pub enum CompactionStrategy { # [doc = " Retention based on percentage of tokens"] Evict (f64) , # [doc = " Retention based on fixed tokens"] Retain (usize) , # [doc = " Selects the strategy with minimum retention"] Min (Box < CompactionStrategy > , Box < CompactionStrategy >) , # [doc = " Selects the strategy with maximum retention"] Max (Box < CompactionStrategy > , Box < CompactionStrategy >) , }