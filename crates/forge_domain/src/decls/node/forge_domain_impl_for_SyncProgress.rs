use derive_more :: Display ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; use crate :: WorkspaceId ; use pretty_assertions :: assert_eq ; use super :: * ; impl SyncProgress { # [doc = " Returns the progress weight (0-100) for this event."] pub fn weight (& self) -> Option < u64 > { match self { Self :: Syncing { current , total } => { let sync_progress = if * total > 0 { (* current as f64) / (* total as f64) * 100.0 } else { 0.0 } ; Some (sync_progress as u64) } _ => None , } } }