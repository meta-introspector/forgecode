use derive_more :: Display ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; use crate :: WorkspaceId ; use pretty_assertions :: assert_eq ; use super :: * ; impl UserId { # [doc = " Generate a new random user ID"] pub fn generate () -> Self { Self (Uuid :: new_v4 ()) } # [doc = " Parse a user ID from a string"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the string is not a valid UUID"] pub fn from_string (s : & str) -> anyhow :: Result < Self > { Ok (Self (Uuid :: parse_str (s) ?)) } }