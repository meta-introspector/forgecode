use derive_more :: Display ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; use crate :: WorkspaceId ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " A search result with its similarity score"] # [doc = ""] # [doc = " Wraps a code node with its semantic search scores,"] # [doc = " keeping the scores separate from the node data itself."] # [derive (Debug , Clone , PartialEq , serde :: Serialize , serde :: Deserialize , derive_setters :: Setters ,)] # [setters (strip_option)] pub struct Node { # [doc = " Node identifier"] pub node_id : NodeId , # [doc = " The node data (file, chunk, note, etc.)"] # [serde (flatten)] pub node : NodeData , # [doc = " Relevance score (most important ranking metric)"] pub relevance : Option < f32 > , # [doc = " Distance score (second ranking metric, lower is better)"] pub distance : Option < f32 > , }