use derive_more :: Display ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; use crate :: WorkspaceId ; use pretty_assertions :: assert_eq ; use super :: * ; impl NodeId { # [doc = " Create a new node ID from a string"] pub fn new (id : impl Into < String >) -> Self { Self (id . into ()) } # [doc = " Get the node ID as a string slice"] pub fn as_str (& self) -> & str { & self . 0 } }