use super :: Transformer ; use crate :: { Context , ContextMessage , ModelId , Role , TextMessage } ; use insta :: assert_yaml_snapshot ; use pretty_assertions :: assert_eq ; use serde :: Serialize ; use super :: * ; use crate :: { Image , ToolCallFull , ToolCallId , ToolName , ToolOutput , ToolResult , ToolValue } ; impl Transformer for TransformToolCalls { type Value = Context ; fn transform (& mut self , mut value : Self :: Value) -> Self :: Value { let mut new_messages = Vec :: new () ; for message in value . messages . into_iter () { match & * message { ContextMessage :: Text (text_msg) if text_msg . role == Role :: Assistant && text_msg . tool_calls . is_some () => { new_messages . push (ContextMessage :: Text (TextMessage { role : text_msg . role , content : text_msg . content . clone () , raw_content : text_msg . raw_content . clone () , tool_calls : None , thought_signature : text_msg . thought_signature . clone () , reasoning_details : text_msg . reasoning_details . clone () , model : text_msg . model . clone () , droppable : text_msg . droppable , phase : text_msg . phase , }) . into () ,) ; } ContextMessage :: Tool (tool_result) => { for output_value in tool_result . output . values . clone () { match output_value { crate :: ToolValue :: Text (text) => { new_messages . push (ContextMessage :: user (text , self . model . clone ()) . into ()) ; } crate :: ToolValue :: Image (image) => { new_messages . push (ContextMessage :: Image (image) . into ()) ; } crate :: ToolValue :: Empty => { } crate :: ToolValue :: AI { value , .. } => new_messages . push (ContextMessage :: user (value , self . model . clone ()) . into ()) , } } } _ => { new_messages . push (message) ; } } } value . messages = new_messages ; value . tools = Vec :: new () ; value } }