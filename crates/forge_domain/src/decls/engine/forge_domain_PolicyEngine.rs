use super :: operation :: PermissionOperation ; use super :: policy :: Policy ; use crate :: PolicyConfig ; use crate :: policies :: Permission ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: { ExecuteRule , Fetch , Permission , Policy , PolicyConfig , ReadRule , Rule , WriteRule } ; # [doc = " High-level policy engine that provides convenient methods for checking"] # [doc = " policies"] # [doc = ""] # [doc = " This wrapper around Workflow provides easy-to-use methods for services to"] # [doc = " check if operations are allowed without having to construct Operation enums"] # [doc = " manually."] pub struct PolicyEngine < 'a > { policies : & 'a PolicyConfig , }