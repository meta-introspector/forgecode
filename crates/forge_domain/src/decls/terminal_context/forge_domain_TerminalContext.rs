# [doc = " Structured terminal context captured by the shell plugin."] # [doc = ""] # [doc = " Each field corresponds to one of the environment variables exported by the"] # [doc = " zsh plugin before invoking forge:"] # [doc = " - `_FORGE_TERM_COMMANDS`   — `\\x1F`-separated command strings"] # [doc = " - `_FORGE_TERM_EXIT_CODES` — `\\x1F`-separated exit codes"] # [doc = " - `_FORGE_TERM_TIMESTAMPS` — `\\x1F`-separated Unix timestamps"] # [derive (Debug , Clone , PartialEq , Eq , Default , serde :: Serialize , serde :: Deserialize)] pub struct TerminalContext { # [doc = " Ordered list of recent commands, from oldest to newest."] pub commands : Vec < TerminalCommand > , }