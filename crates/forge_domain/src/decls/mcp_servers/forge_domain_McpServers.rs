use std :: collections :: { HashMap , hash_map } ; use serde :: { Deserialize , Serialize } ; use crate :: { ServerName , ToolDefinition } ; # [doc = " Cache for MCP tool definitions"] # [doc = ""] # [doc = " Simplified cache structure that stores only the essential data."] # [doc = " Validation and TTL checking are handled by the infrastructure layer"] # [doc = " using cacache's built-in metadata capabilities."] # [derive (Default , Clone , Serialize , Deserialize , Debug , PartialEq , derive_setters :: Setters)] # [serde (rename_all = "camelCase")] # [setters (strip_option , into)] pub struct McpServers { # [doc = " Successfully loaded MCP servers with their tools"] servers : HashMap < ServerName , Vec < ToolDefinition > > , # [doc = " Failed MCP servers with their error messages"] # [serde (default)] failures : HashMap < ServerName , String > , }