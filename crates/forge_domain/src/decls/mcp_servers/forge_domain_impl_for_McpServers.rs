use std :: collections :: { HashMap , hash_map } ; use serde :: { Deserialize , Serialize } ; use crate :: { ServerName , ToolDefinition } ; impl McpServers { # [doc = " Create a new cache entry with servers and failures"] pub fn new (servers : HashMap < ServerName , Vec < ToolDefinition > > , failures : HashMap < ServerName , String > ,) -> Self { Self { servers , failures } } # [doc = " Get the successful servers"] pub fn get_servers (& self) -> & HashMap < ServerName , Vec < ToolDefinition > > { & self . servers } # [doc = " Get the failed servers"] pub fn get_failures (& self) -> & HashMap < ServerName , String > { & self . failures } }