use super :: Transformer ; use crate :: { Context , ContextMessage } ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; use crate :: { Role , TextMessage , ToolCallArguments , ToolCallFull , ToolCallId , ToolName } ; # [doc = " Normalizes tool call arguments before provider-specific request conversion."] # [doc = ""] # [doc = " This transformer repairs assistant tool calls that were persisted with"] # [doc = " `Unparsed` string arguments, such as resumed conversations originating from"] # [doc = " providers that emitted stringified or malformed JSON arguments. It converts"] # [doc = " those arguments into `Parsed` JSON values so downstream DTO builders and"] # [doc = " provider transforms operate on a consistent structure."] pub struct NormalizeToolCallArguments ;