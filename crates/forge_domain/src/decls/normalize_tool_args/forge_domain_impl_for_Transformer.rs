use super :: Transformer ; use crate :: { Context , ContextMessage } ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; use crate :: { Role , TextMessage , ToolCallArguments , ToolCallFull , ToolCallId , ToolName } ; impl Transformer for NormalizeToolCallArguments { type Value = Context ; fn transform (& mut self , mut value : Self :: Value) -> Self :: Value { for entry in & mut value . messages { if let ContextMessage :: Text (text_msg) = & mut entry . message && let Some (ref mut tool_calls) = text_msg . tool_calls { for tool_call in tool_calls . iter_mut () { let args = std :: mem :: take (& mut tool_call . arguments) ; tool_call . arguments = args . normalize () ; } } } value } }