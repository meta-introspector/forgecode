use std :: hash :: { DefaultHasher , Hash , Hasher } ; use std :: path :: PathBuf ; use derive_more :: Display ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use crate :: { Effort , ModelConfig } ; use fake :: { Fake , Faker } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Represents the minimal runtime environment in which the application is"] # [doc = " running."] # [doc = ""] # [doc = " Contains only the five fields that cannot be sourced from [`ForgeConfig`]:"] # [doc = " `os`, `cwd`, `home`, `shell`, and `base_path`. All configuration"] # [doc = " values previously carried here are now accessed through"] # [doc = " `EnvironmentInfra::get_config()`."] # [derive (Debug , Setters , Clone , PartialEq , Serialize , Deserialize , fake :: Dummy)] # [serde (rename_all = "camelCase")] # [setters (strip_option)] pub struct Environment { # [doc = " The operating system of the environment."] pub os : String , # [doc = " The current working directory."] pub cwd : PathBuf , # [doc = " The home directory."] pub home : Option < PathBuf > , # [doc = " The shell being used."] pub shell : String , # [doc = " The base path relative to which everything else is stored."] pub base_path : PathBuf , }