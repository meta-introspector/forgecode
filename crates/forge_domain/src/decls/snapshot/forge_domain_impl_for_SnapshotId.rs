use std :: fmt :: { Display , Formatter } ; use std :: hash :: Hasher ; use std :: path :: PathBuf ; use std :: time :: { Duration , SystemTime , UNIX_EPOCH } ; use serde :: { Deserialize , Serialize } ; use uuid :: Uuid ; use super :: * ; impl SnapshotId { # [doc = " Create a new random SnapshotId"] pub fn new () -> Self { Self (Uuid :: new_v4 ()) } # [doc = " Parse a SnapshotId from a string"] pub fn parse (s : & str) -> Option < Self > { Uuid :: parse_str (s) . ok () . map (Self) } # [doc = " Get the underlying UUID"] pub fn uuid (& self) -> & Uuid { & self . 0 } }