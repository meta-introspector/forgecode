use std :: fmt ; use std :: ops :: Deref ; use schemars :: JsonSchema ; use serde :: { Deserialize , Deserializer , Serialize , Serializer } ; use serde :: de :: Error ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; # [doc = " A newtype for top_p values with built-in validation"] # [doc = ""] # [doc = " Top-p (nucleus sampling) controls the diversity of the model's output:"] # [doc = " - Lower values (e.g., 0.1) make responses more focused by considering only"] # [doc = "   the most probable tokens"] # [doc = " - Higher values (e.g., 0.9) make responses more diverse by considering a"] # [doc = "   broader range of tokens"] # [doc = " - Valid range is 0.0 to 1.0"] # [derive (Debug , Clone , Copy , PartialEq , PartialOrd , JsonSchema)] pub struct TopP (f32) ;