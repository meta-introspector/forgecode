use std :: fmt :: { Display , Formatter } ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; # [doc = " Permission types that can be applied to operations"] # [derive (Debug , Clone , PartialEq , Eq , PartialOrd , Ord , Serialize , Deserialize , JsonSchema)] # [serde (rename_all = "camelCase")] pub enum Permission { # [doc = " Allow the operation without asking"] Allow , # [doc = " Deny the operation without asking"] Deny , # [doc = " Confirm with the user before allowing"] Confirm , }