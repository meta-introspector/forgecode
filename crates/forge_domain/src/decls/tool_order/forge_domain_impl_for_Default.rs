use std :: cmp :: Ordering ; use std :: collections :: HashMap ; use std :: sync :: Arc ; use glob :: Pattern ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use crate :: { ToolDefinition , ToolName } ; use pretty_assertions :: assert_eq ; use super :: * ; impl Default for ToolOrder { fn default () -> Self { Self { tools : Vec :: new () , weights : Arc :: new (HashMap :: new ()) , patterns : Arc :: new (Vec :: new ()) , } } }