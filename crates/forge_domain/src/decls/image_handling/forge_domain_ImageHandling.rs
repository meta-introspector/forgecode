use super :: Transformer ; use crate :: { Context , ContextMessage } ; use insta :: assert_yaml_snapshot ; use pretty_assertions :: assert_eq ; use serde :: Serialize ; use super :: * ; use crate :: { Image , ToolCallId , ToolName , ToolOutput , ToolResult , ToolValue } ; # [doc = " Transformer that handles image processing in tool results"] # [doc = " Converts image outputs from tool results into separate user messages with"] # [doc = " image attachments"] pub struct ImageHandling ;