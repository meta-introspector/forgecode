use super :: Transformer ; use crate :: { Context , ContextMessage } ; use insta :: assert_yaml_snapshot ; use pretty_assertions :: assert_eq ; use serde :: Serialize ; use super :: * ; use crate :: { Image , ToolCallId , ToolName , ToolOutput , ToolResult , ToolValue } ; impl Transformer for ImageHandling { type Value = Context ; fn transform (& mut self , mut value : Self :: Value) -> Self :: Value { let mut images = Vec :: new () ; value . messages . iter_mut () . filter_map (| message | { if let ContextMessage :: Tool (tool_result) = & mut * * message { Some (tool_result) } else { None } }) . flat_map (| tool_result | tool_result . output . values . iter_mut ()) . for_each (| output_value | match output_value { crate :: ToolValue :: Image (image) => { let image = std :: mem :: take (image) ; let id = images . len () ; * output_value = crate :: ToolValue :: Text (format ! ("[The image with ID {id} will be sent as an attachment in the next message]")) ; images . push ((id , image)) ; } crate :: ToolValue :: Text (_) => { } crate :: ToolValue :: Empty => { } crate :: ToolValue :: AI { .. } => { } }) ; images . into_iter () . for_each (| (id , image) | { value . messages . push (ContextMessage :: user (format ! ("[Here is the image attachment for ID {id}]") , None) . into () ,) ; value . messages . push (ContextMessage :: Image (image) . into ()) ; }) ; value } }