use std :: fmt :: Display ; use super :: * ; impl < 'a > Display for NumberedContent < 'a > { fn fmt (& self , f : & mut std :: fmt :: Formatter < '_ >) -> std :: fmt :: Result { let lines : Vec < & str > = self . raw_content . lines () . collect () ; if lines . is_empty () { return Ok (()) ; } let max_line_number = self . start + lines . len () - 1 ; let width = max_line_number . to_string () . len () ; let start = self . start ; let last = lines . len () - 1 ; for (i , line) in lines . into_iter () . enumerate () { if i < last { writeln ! (f , "{:>width$}:{}" , start + i , line , width = width) ? ; } else { write ! (f , "{:>width$}:{}" , start + i , line , width = width) ? ; } } Ok (()) } }