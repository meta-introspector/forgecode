use std :: path :: PathBuf ; use crate :: ProviderId ; use std :: path :: PathBuf ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Result of credential migration from environment variables to file."] # [doc = " Only returned when credentials were actually migrated (Some)."] # [doc = " None indicates file already exists or no credentials to migrate."] # [derive (Debug , Clone)] pub struct MigrationResult { # [doc = " Path to the credentials file"] pub credentials_path : PathBuf , # [doc = " Providers that were migrated"] pub migrated_providers : Vec < ProviderId > , }