use std :: fmt ; use std :: ops :: Deref ; use schemars :: JsonSchema ; use serde :: { Deserialize , Deserializer , Serialize , Serializer } ; use serde :: de :: Error ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; # [doc = " A newtype for max_tokens values with built-in validation"] # [doc = ""] # [doc = " Max tokens controls the maximum number of tokens the model can generate:"] # [doc = " - Lower values (e.g., 100) limit response length for concise outputs"] # [doc = " - Higher values (e.g., 4000) allow for longer, more detailed responses"] # [doc = " - Valid range is 1 to 100,000 (reasonable upper bound for most models)"] # [doc = " - If not specified, the model provider's default will be used"] # [derive (Debug , Clone , Copy , PartialEq , PartialOrd , JsonSchema)] pub struct MaxTokens (u32) ;