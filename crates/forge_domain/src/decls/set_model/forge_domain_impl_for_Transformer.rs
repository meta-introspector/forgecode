use super :: Transformer ; use crate :: { Context , ModelId } ; use insta :: assert_yaml_snapshot ; use pretty_assertions :: assert_eq ; use serde :: Serialize ; use super :: * ; use crate :: { ContextMessage , Role , TextMessage } ; impl Transformer for SetModel { type Value = Context ; fn transform (& mut self , mut value : Self :: Value) -> Self :: Value { for message in value . messages . iter_mut () { if let crate :: ContextMessage :: Text (text_msg) = & mut * * message && text_msg . model . is_none () { text_msg . model = Some (self . model . clone ()) ; } } value } }