use std :: fmt :: { Display , Formatter } ; use std :: path :: Path ; use glob :: Pattern ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use super :: operation :: PermissionOperation ; use std :: path :: PathBuf ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Helper function to match a glob pattern against a path or string"] fn match_pattern < P : AsRef < Path > > (pattern : & str , target : P) -> bool { match Pattern :: new (pattern) { Ok (glob_pattern) => { let target_str = target . as_ref () . to_string_lossy () ; glob_pattern . matches (& target_str) } Err (_) => false , } }