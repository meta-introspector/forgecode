use std :: fmt :: { Display , Formatter } ; use std :: path :: Path ; use glob :: Pattern ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use super :: operation :: PermissionOperation ; use std :: path :: PathBuf ; use pretty_assertions :: assert_eq ; use super :: * ; impl Display for Rule { fn fmt (& self , f : & mut Formatter < '_ >) -> std :: fmt :: Result { match self { Rule :: Write (rule) => write ! (f , "{rule}") , Rule :: Read (rule) => write ! (f , "{rule}") , Rule :: Execute (rule) => write ! (f , "{rule}") , Rule :: Fetch (rule) => write ! (f , "{rule}") , } } }