use std :: fmt :: { Display , Formatter } ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use super :: operation :: PermissionOperation ; use super :: rule :: Rule ; use super :: types :: Permission ; use std :: path :: PathBuf ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: WriteRule ; # [doc = " Policy definitions with logical operators"] # [derive (Debug , Clone , PartialEq , Eq , PartialOrd , Ord , Serialize , Deserialize , JsonSchema)] # [serde (untagged)] # [serde (rename_all = "camelCase")] pub enum Policy { # [doc = " Simple policy with permission and rule"] Simple { permission : Permission , rule : Rule } , # [doc = " Logical AND of two policies"] All { all : Vec < Policy > } , # [doc = " Logical OR of two policies"] Any { any : Vec < Policy > } , # [doc = " Logical NOT of a policy"] Not { not : Box < Policy > } , }