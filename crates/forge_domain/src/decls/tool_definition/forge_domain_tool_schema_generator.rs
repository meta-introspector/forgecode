use derive_setters :: Setters ; use schemars :: Schema ; use schemars :: generate :: SchemaGenerator ; use schemars :: transform :: { Transform , transform_subschemas } ; use serde :: { Deserialize , Serialize } ; use crate :: ToolName ; use pretty_assertions :: assert_eq ; use schemars :: JsonSchema ; use serde :: Deserialize as SerdeDeserialize ; use super :: * ; # [doc = " Returns a [`SchemaGenerator`] whose settings include [`RemoveSchemaTitles`]"] # [doc = " as a registered transform."] # [doc = ""] # [doc = " All schemas produced via this generator will never contain `title` fields,"] # [doc = " eliminating the need for any post-hoc stripping."] pub fn tool_schema_generator () -> SchemaGenerator { schemars :: generate :: SchemaSettings :: default () . with (| s | { s . transforms . push (Box :: new (RemoveSchemaTitles)) ; }) . into_generator () }