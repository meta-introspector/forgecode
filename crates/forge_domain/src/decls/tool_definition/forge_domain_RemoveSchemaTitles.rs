use derive_setters :: Setters ; use schemars :: Schema ; use schemars :: generate :: SchemaGenerator ; use schemars :: transform :: { Transform , transform_subschemas } ; use serde :: { Deserialize , Serialize } ; use crate :: ToolName ; use pretty_assertions :: assert_eq ; use schemars :: JsonSchema ; use serde :: Deserialize as SerdeDeserialize ; use super :: * ; # [doc = " A schemars [`Transform`] that recursively removes the `title` field from"] # [doc = " every schema node."] # [doc = ""] # [doc = " Rust type names are emitted as `title` by the `JsonSchema` derive. These"] # [doc = " are internal implementation details and must not be forwarded to LLM"] # [doc = " provider APIs."] # [derive (Debug , Clone , Default)] pub struct RemoveSchemaTitles ;