use async_trait :: async_trait ; use derive_more :: From ; use derive_setters :: Setters ; use crate :: { Agent , ChatCompletionMessageFull , Conversation , ModelId , ToolCallFull , ToolResult } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: { Agent , AgentId , Conversation , ModelId , ProviderId } ; # [doc = " Trait for handling lifecycle events"] # [doc = ""] # [doc = " Implementations of this trait can be used to react to different"] # [doc = " stages of conversation processing."] # [async_trait] pub trait EventHandle < T : Send + Sync > : Send + Sync { # [doc = " Handles a lifecycle event and potentially modifies the conversation"] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `event` - The lifecycle event that occurred"] # [doc = " * `conversation` - The current conversation state (mutable)"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the event handling fails"] async fn handle (& self , event : & T , conversation : & mut Conversation) -> anyhow :: Result < () > ; }