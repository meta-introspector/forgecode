use async_trait :: async_trait ; use derive_more :: From ; use derive_setters :: Setters ; use crate :: { Agent , ChatCompletionMessageFull , Conversation , ModelId , ToolCallFull , ToolResult } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: { Agent , AgentId , Conversation , ModelId , ProviderId } ; # [doc = " A handler that combines two event handlers with sequential execution"] # [doc = ""] # [doc = " Runs the first handler, then runs the second handler."] # [doc = ""] # [doc = " This is used internally by the `Hook::zip` and `EventHandleExt::and`"] # [doc = " methods."] struct CombinedHandler < T : Send + Sync > (Box < dyn EventHandle < T > > , Box < dyn EventHandle < T > >) ;