use async_trait :: async_trait ; use derive_more :: From ; use derive_setters :: Setters ; use crate :: { Agent , ChatCompletionMessageFull , Conversation , ModelId , ToolCallFull , ToolResult } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: { Agent , AgentId , Conversation , ModelId , ProviderId } ; # [doc = " Extension trait for combining event handlers"] # [doc = ""] # [doc = " This trait provides methods to combine multiple event handlers into a single"] # [doc = " handler that executes them in sequence."] pub trait EventHandleExt < T : Send + Sync > : EventHandle < T > { # [doc = " Combines this handler with another handler, creating a new handler that"] # [doc = " runs both in sequence"] # [doc = ""] # [doc = " When an event is handled, both handlers run in sequence."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `other` - Another handler to combine with this one"] # [doc = ""] # [doc = " # Returns"] # [doc = " A new boxed handler that combines both handlers"] fn and < H : EventHandle < T > + 'static > (self , other : H) -> Box < dyn EventHandle < T > > where Self : Sized + 'static ; }