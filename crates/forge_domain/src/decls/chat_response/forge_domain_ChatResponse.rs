use std :: collections :: HashMap ; use std :: sync :: Arc ; use std :: time :: Duration ; use chrono :: Local ; use tokio :: sync :: Notify ; use crate :: { ToolCallFull , ToolName , ToolResult } ; use chrono :: { DateTime , Utc } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Events that are emitted by the agent for external consumption. This includes"] # [doc = " events for all internal state changes."] # [derive (Debug , Clone)] pub enum ChatResponse { TaskMessage { content : ChatResponseContent , } , TaskReasoning { content : String , } , TaskComplete , ToolCallStart { tool_call : ToolCallFull , notifier : Arc < Notify > , } , ToolCallEnd (ToolResult) , RetryAttempt { cause : Cause , duration : Duration , } , Interrupt { reason : InterruptionReason , } , }