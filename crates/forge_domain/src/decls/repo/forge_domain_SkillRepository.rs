use std :: path :: Path ; use anyhow :: Result ; use url :: Url ; use crate :: { AnyProvider , AuthCredential , ChatCompletionMessage , Context , Conversation , ConversationId , MigrationResult , Model , ModelId , Provider , ProviderId , ProviderTemplate , ResultStream , SearchMatch , Skill , Snapshot , WorkspaceAuth , WorkspaceId , } ; # [doc = " Repository for managing skills"] # [doc = ""] # [doc = " This repository provides operations for loading and managing skills from"] # [doc = " markdown files."] # [async_trait :: async_trait] pub trait SkillRepository : Send + Sync { # [doc = " Loads all available skills from the skills directory"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if skill loading fails"] async fn load_skills (& self) -> Result < Vec < Skill > > ; }