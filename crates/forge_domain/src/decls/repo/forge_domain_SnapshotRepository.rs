use std :: path :: Path ; use anyhow :: Result ; use url :: Url ; use crate :: { AnyProvider , AuthCredential , ChatCompletionMessage , Context , Conversation , ConversationId , MigrationResult , Model , ModelId , Provider , ProviderId , ProviderTemplate , ResultStream , SearchMatch , Skill , Snapshot , WorkspaceAuth , WorkspaceId , } ; # [doc = " Repository for managing file snapshots"] # [doc = ""] # [doc = " This repository provides operations for creating and restoring file"] # [doc = " snapshots, enabling undo functionality for file modifications."] # [async_trait :: async_trait] pub trait SnapshotRepository : Send + Sync { # [doc = " Inserts a new snapshot for the given file path"] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `file_path` - Path to the file to snapshot"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the snapshot creation fails"] async fn insert_snapshot (& self , file_path : & Path) -> Result < Snapshot > ; # [doc = " Restores the most recent snapshot for the given file path"] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `file_path` - Path to the file to restore"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if no snapshot exists or restoration fails"] async fn undo_snapshot (& self , file_path : & Path) -> Result < () > ; }