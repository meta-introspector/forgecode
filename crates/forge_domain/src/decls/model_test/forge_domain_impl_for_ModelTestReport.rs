use std :: time :: Duration ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use crate :: { ModelId , ProviderId } ; impl ModelTestReport { # [doc = " Creates a new report from the given results, sorted by score descending."] pub fn new (mut results : Vec < ModelTestResult >) -> Self { results . sort_by (| a , b | { b . score . partial_cmp (& a . score) . unwrap_or (std :: cmp :: Ordering :: Equal) }) ; Self { results } } }