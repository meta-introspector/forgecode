use std :: collections :: BTreeSet ; use std :: fmt :: { Display , Formatter } ; use schemars :: JsonSchema ; use serde :: { Deserialize , Serialize } ; use super :: operation :: PermissionOperation ; use super :: policy :: Policy ; use super :: types :: Permission ; use crate :: Rule ; use std :: path :: PathBuf ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: { Permission , PermissionOperation , Policy , Rule , WriteRule } ; use crate :: policies :: { Permission , Policy , PolicyConfig , Rule } ; impl Display for PolicyConfig { fn fmt (& self , f : & mut Formatter < '_ >) -> std :: fmt :: Result { if self . policies . is_empty () { write ! (f , "No policies defined") } else { let policies : Vec < String > = self . policies . iter () . map (| p | format ! ("• {p}")) . collect () ; write ! (f , "Policies:\n{}" , policies . join ("\n")) } } }