use forge_domain :: { ChatCompletionMessage , ChatResponse , Content , EventValue , FinishReason , ReasoningConfig , Role , ToolCallArguments , ToolCallFull , ToolOutput , ToolResult , } ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use crate :: orch_spec :: orch_runner :: TestContext ; use forge_domain :: { Metrics , Todo , TodoStatus } ; use forge_domain :: { Metrics , Todo , TodoStatus } ; use forge_domain :: Metrics ; # [tokio :: test] async fn test_todo_enforcement_injects_reminder () { use forge_domain :: { Metrics , Todo , TodoStatus } ; let mut ctx = TestContext :: default () . mock_assistant_responses (vec ! [ChatCompletionMessage :: assistant (Content :: full ("Task is done")) . finish_reason (FinishReason :: Stop) , ChatCompletionMessage :: assistant (Content :: full ("I see there are pending todos. Let me continue." ,)) . finish_reason (FinishReason :: Stop) ,]) . initial_metrics (Metrics :: default () . todos (vec ! [Todo :: new ("Pending task 1") . status (TodoStatus :: Pending) , Todo :: new ("In progress task") . status (TodoStatus :: InProgress) ,])) ; ctx . run ("Complete this task") . await . unwrap () ; let messages = ctx . output . context_messages () ; let reminder = messages . iter () . filter_map (| entry | entry . message . content ()) . find (| content | content . contains ("pending todo items")) ; assert ! (reminder . is_some () , "Should have a reminder message about pending todos") ; let actual = reminder . unwrap () ; assert ! (actual . contains ("- [PENDING] Pending task 1") , "Reminder should list pending items with status") ; assert ! (actual . contains ("- [IN_PROGRESS] In progress task") , "Reminder should list in-progress items with status") ; }