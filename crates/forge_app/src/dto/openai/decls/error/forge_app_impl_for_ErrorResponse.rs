use std :: collections :: BTreeMap ; use derive_setters :: Setters ; use serde :: { Deserialize , Serialize } ; use thiserror :: Error ; use pretty_assertions :: assert_eq ; use super :: * ; impl ErrorResponse { # [doc = " Deeply introspects the error structure to determine the ErrorCode"] pub fn get_code_deep (& self) -> Option < & ErrorCode > { if let Some (ref code) = self . code { return Some (code) ; } if let Some (ref error) = self . error { return error . get_code_deep () ; } None } }