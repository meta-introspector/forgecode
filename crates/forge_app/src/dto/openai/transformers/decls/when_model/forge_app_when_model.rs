use regex :: Regex ; use crate :: dto :: openai :: Request ; use forge_domain :: { ModelId , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: Request ; # [doc = " Creates a condition function that matches requests when the model name"] # [doc = " matches the given regex pattern."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `pattern` - A regex pattern to match against the model name"] # [doc = ""] # [doc = " # Returns"] # [doc = " A function that returns true when the model name matches the pattern."] # [doc = ""] # [doc = " # Examples"] # [doc = " ```rust,ignore"] # [doc = " // Apply transformation only for Claude models"] # [doc = " let conditional_transformer = my_transformer.when(when_model(\"claude\"));"] # [doc = " ```"] pub fn when_model (pattern : & str) -> impl Fn (& Request) -> bool { let regex = Regex :: new (pattern) . unwrap_or_else (| _ | panic ! ("Invalid regex pattern: {pattern}")) ; move | req : & Request | { req . model . as_ref () . map (| name | regex . is_match (name . as_str ())) . unwrap_or (true) } }