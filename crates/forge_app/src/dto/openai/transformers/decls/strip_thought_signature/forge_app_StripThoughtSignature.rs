use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use forge_domain :: { ModelId , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { ExtraContent , FunctionCall , FunctionType , GoogleMetadata , Message , MessageContent , Role , ToolCall , } ; # [doc = " Strips thought signatures from request messages."] # [doc = ""] # [doc = " This transformer removes the `extra_content` field from all messages,"] # [doc = " which contains Google-specific thought signatures. This should be applied"] # [doc = " to models that don't support thought signatures (all models except gemini3)."] pub struct StripThoughtSignature ;