use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use forge_domain :: { ModelId , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { ExtraContent , FunctionCall , FunctionType , GoogleMetadata , Message , MessageContent , Role , ToolCall , } ; impl Transformer for StripThoughtSignature { type Value = Request ; fn transform (& mut self , mut request : Self :: Value) -> Self :: Value { if let Some (messages) = request . messages . as_mut () { for message in messages . iter_mut () { message . extra_content = None ; if let Some (tool_calls) = message . tool_calls . as_mut () { for tool_call in tool_calls . iter_mut () { tool_call . extra_content = None ; } } } } request } }