use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use std :: collections :: HashSet ; use forge_domain :: { Context , ContextMessage , ModelId , Role , TextMessage } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Transformer that implements a simple two-breakpoint cache strategy:"] # [doc = " - Always caches the first message in the conversation"] # [doc = " - Always caches the last message in the conversation"] # [doc = " - Removes cache control from the second-to-last message"] pub struct SetCache ;