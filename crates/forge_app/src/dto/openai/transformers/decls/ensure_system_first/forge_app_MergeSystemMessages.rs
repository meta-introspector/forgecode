use forge_domain :: Transformer ; use crate :: dto :: openai :: { Message , MessageContent , Request , Role } ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { Message , MessageContent , Role } ; # [doc = " Merges all system messages into a single system message at the beginning of"] # [doc = " the messages array."] # [doc = ""] # [doc = " Some providers (e.g. NVIDIA, vLLM) reject requests with multiple system"] # [doc = " messages or system messages that are not positioned at the start of the"] # [doc = " conversation."] pub struct MergeSystemMessages ;