use forge_domain :: { Effort , Transformer } ; use crate :: dto :: openai :: Request ; use forge_domain :: { Effort , ReasoningConfig } ; use pretty_assertions :: assert_eq ; use super :: * ; impl Transformer for SetReasoningEffort { type Value = Request ; fn transform (& mut self , mut request : Self :: Value) -> Self :: Value { if let Some (reasoning) = request . reasoning . take () { let effort = if reasoning . enabled == Some (false) { Some ("none" . to_string ()) } else if let Some (effort) = reasoning . effort { Some (effort . to_string ()) } else if reasoning . enabled == Some (true) { Some (Effort :: Medium . to_string ()) } else { None } ; request . reasoning_effort = effort ; request . reasoning = None ; } request } }