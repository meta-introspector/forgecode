use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use forge_domain :: ModelId ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Transformer that applies minimax-specific parameter adjustments"] # [doc = ""] # [doc = " Minimax M2 models require specific temperature, top_p, and top_k values"] # [doc = " for optimal performance:"] # [doc = " - Temperature: 1.0"] # [doc = " - Top P: 0.95"] # [doc = " - Top K: 40 (for m2.1), 20 (for other m2 models)"] pub struct SetMinimaxParams ;