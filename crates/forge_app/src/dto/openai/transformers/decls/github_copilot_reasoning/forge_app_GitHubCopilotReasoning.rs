use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { Message , MessageContent , ReasoningDetail , Request , Role } ; # [doc = " Transformer that converts reasoning_details to GitHub Copilot's flat format."] # [doc = ""] # [doc = " GitHub Copilot uses `reasoning_text` and `reasoning_opaque` fields directly"] # [doc = " in messages instead of the OpenRouter-style `reasoning_details` array."] # [doc = ""] # [doc = " This transformer:"] # [doc = " 1. Extracts `reasoning.text` type entries → `reasoning_text`"] # [doc = " 2. Extracts `reasoning.encrypted` type entries → `reasoning_opaque`"] # [doc = " 3. Removes `reasoning_details` array"] pub struct GitHubCopilotReasoning ;