use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { Message , MessageContent , Request , Role } ; # [doc = " Transformer that ensures every message has a `reasoning_content` field set."] # [doc = ""] # [doc = " DeepSeek requires the `reasoning_content` field to be present on assistant"] # [doc = " messages even when there is no reasoning text. When the field is `None` this"] # [doc = " transformer falls back to an empty string."] pub struct DefaultReasoningContent ;