use forge_domain :: Transformer ; use crate :: dto :: openai :: { Request , ResponseFormat } ; use crate :: utils :: enforce_strict_schema ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; use crate :: dto :: openai :: { FunctionDescription , FunctionType , Tool } ; # [doc = " Enforces strict JSON schema compatibility for response_format schemas."] # [doc = ""] # [doc = " This is used for OpenAI-compatible providers that require the same strict"] # [doc = " JSON Schema subset for structured outputs as they do for tool parameters."] pub struct EnforceStrictResponseFormatSchema ;