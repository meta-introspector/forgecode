use forge_domain :: Transformer ; use crate :: dto :: openai :: Request ; use pretty_assertions :: assert_eq ; use super :: * ; use crate :: dto :: openai :: { Message , MessageContent , ReasoningDetail , Request , Role } ; # [doc = " Transformer that converts structured reasoning details to the flat"] # [doc = " `reasoning_content` format."] # [doc = ""] # [doc = " Some OpenAI-compatible providers expect replayed reasoning to be sent as a"] # [doc = " flat `reasoning_content` string field instead of the OpenRouter-style"] # [doc = " `reasoning_details` array."] # [doc = ""] # [doc = " This transformer:"] # [doc = " 1. Extracts `reasoning.text` type entries into `reasoning_content`"] # [doc = " 2. Removes `reasoning_details` because target providers do not accept it"] pub struct ReasoningContent ;