use forge_domain :: Transformer ; use crate :: dto :: anthropic :: Request ; use std :: collections :: HashSet ; use forge_domain :: { Context , ContextMessage , ModelId , Role , TextMessage } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Transformer that keeps Anthropic prompt-cache markers stable:"] # [doc = " - Always caches every system message so the static system prefix remains"] # [doc = "   reusable"] # [doc = " - Falls back to caching the first conversation message when there is no"] # [doc = "   system prompt so single-turn requests still establish a reusable prefix"] # [doc = " - Uses exactly one rolling message-level marker on the newest message"] pub struct SetCache ;