use forge_domain :: Transformer ; use crate :: dto :: anthropic :: Request ; use forge_domain :: { Context , ContextMessage , ModelId , ToolDefinition , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Converts MCP tool names from Forge's internal format"] # [doc = " (`mcp_{server}_tool_{tool}`) to the Claude Code compatible format"] # [doc = " (`mcp__{server}__{tool}`)."] # [doc = ""] # [doc = " Claude Code expects MCP tools to use double-underscore separators between"] # [doc = " the `mcp` prefix, server name, and tool name. This transformer is applied"] # [doc = " only when sending requests to the Claude Code provider (OAuth-authenticated"] # [doc = " sessions)."] pub struct McpToolNames ;