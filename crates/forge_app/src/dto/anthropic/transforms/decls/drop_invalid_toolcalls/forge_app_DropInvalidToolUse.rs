use forge_domain :: Transformer ; use serde_json :: json ; use crate :: dto :: anthropic :: { Content , Request } ; use forge_domain :: { Context , ContextMessage , ModelId , Role , TextMessage , ToolCallArguments , ToolCallFull , Transformer , } ; use pretty_assertions :: assert_eq ; use serde_json :: json ; use super :: * ; use crate :: dto :: anthropic :: Request ; # [doc = " Transformer that normalizes ToolUse content to ensure inputs are always"] # [doc = " objects."] # [doc = " - Preserves `Content::Text` and other content types as-is"] # [doc = " - For `Content::ToolUse`:"] # [doc = "   - If input is already an object, keeps it unchanged"] # [doc = "   - If input is None, keeps it as None"] # [doc = "   - If input is non-object (string, array, number, etc.), wraps it in"] # [doc = "     `{\"json\": value}`"] pub struct DropInvalidToolUse ;