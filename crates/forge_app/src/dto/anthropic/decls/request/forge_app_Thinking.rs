use derive_setters :: Setters ; use forge_domain :: { ContextMessage , Image } ; use serde :: { Deserialize , Serialize } ; use forge_domain :: { Context , ReasoningConfig } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Anthropic's `thinking` request field. Opus 4.7 rejects the `Enabled` shape"] # [doc = " and the orchestrator applies model-specific reasoning normalization before"] # [doc = " request conversion."] # [derive (Serialize , Debug , PartialEq , Eq)] # [serde (tag = "type" , rename_all = "lowercase")] pub enum Thinking { Enabled { budget_tokens : u64 , } , Adaptive { # [serde (skip_serializing_if = "Option::is_none")] display : Option < ThinkingDisplay > , } , Disabled , }