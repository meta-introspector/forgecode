use forge_domain :: { Context , Effort , ReasoningConfig , Transformer } ; use tracing :: warn ; use forge_domain :: { Context , Effort , ReasoningConfig , Temperature , TopK , TopP , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; fn replace_xhigh_with_max (reasoning : & mut Option < ReasoningConfig >) { if let Some (reasoning) = reasoning . as_mut () && reasoning . effort == Some (Effort :: XHigh) { reasoning . effort = Some (Effort :: Max) ; } }