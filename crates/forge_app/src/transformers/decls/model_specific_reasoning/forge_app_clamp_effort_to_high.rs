use forge_domain :: { Context , Effort , ReasoningConfig , Transformer } ; use tracing :: warn ; use forge_domain :: { Context , Effort , ReasoningConfig , Temperature , TopK , TopP , Transformer } ; use pretty_assertions :: assert_eq ; use super :: * ; fn clamp_effort_to_high (reasoning : & mut Option < ReasoningConfig >) { if let Some (reasoning) = reasoning . as_mut () && matches ! (reasoning . effort , Some (Effort :: XHigh | Effort :: Max)) { reasoning . effort = Some (Effort :: High) ; } }