use forge_domain :: { ContextSummary , Role , SummaryMessage , SummaryTool , Transformer } ; use forge_domain :: { Role , SummaryBlock , SummaryToolCall , ToolCallId } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Removes redundant operations from the context summary."] # [doc = ""] # [doc = " This transformer deduplicates consecutive operations within assistant"] # [doc = " messages by retaining only the most recent operation for each resource"] # [doc = " (e.g., file path, command). Only applies to messages with the Assistant"] # [doc = " role. This is useful for reducing context size while preserving the final"] # [doc = " state of operations."] pub struct TrimContextSummary ;