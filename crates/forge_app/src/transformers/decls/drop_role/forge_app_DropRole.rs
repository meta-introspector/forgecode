use forge_domain :: { ContextSummary , Role , Transformer } ; use forge_domain :: { SummaryBlock , SummaryMessage as Block , SummaryToolCall } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Drops all messages with a specific role from the context summary."] # [doc = ""] # [doc = " This transformer removes all messages matching the specified role, which is"] # [doc = " useful for reducing context size when certain message types are not needed"] # [doc = " in summaries. For example, system messages containing initial prompts and"] # [doc = " instructions often don't need to be preserved in compacted contexts."] pub struct DropRole { role : Role , }