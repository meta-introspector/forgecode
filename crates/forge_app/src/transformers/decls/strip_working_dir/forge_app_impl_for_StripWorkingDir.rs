use std :: path :: { Path , PathBuf } ; use forge_domain :: { ContextSummary , SummaryMessage , SummaryTool , Transformer } ; use forge_domain :: { Role , SummaryBlock , SummaryMessage as Block , SummaryToolCall } ; use pretty_assertions :: assert_eq ; use super :: * ; impl StripWorkingDir { # [doc = " Creates a new StripWorkingDir transformer with the specified working"] # [doc = " directory."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `working_dir` - The working directory path to strip from file paths"] pub fn new (working_dir : impl Into < PathBuf >) -> Self { Self { working_dir : working_dir . into () } } # [doc = " Strips the working directory prefix from a path if present."] # [doc = ""] # [doc = " Returns the path with the working directory prefix removed, or the"] # [doc = " original path if it doesn't start with the working directory."] fn strip_prefix (& self , path : & str) -> String { Path :: new (path) . strip_prefix (& self . working_dir) . ok () . and_then (| p | p . to_str ()) . map (| s | s . to_string ()) . unwrap_or_else (| | path . to_string ()) } }