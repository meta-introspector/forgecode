use forge_domain :: { ContextSummary , Role , SummaryBlock , Transformer } ; use forge_domain :: { SummaryMessage , SummaryToolCall } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Keeps only the first message in consecutive sequences of a specific role."] # [doc = ""] # [doc = " This transformer processes a context summary and filters out consecutive"] # [doc = " messages of the specified role, keeping only the first one in each sequence."] # [doc = " Messages with other roles are preserved as-is."] pub struct DedupeRole { role : Role , }