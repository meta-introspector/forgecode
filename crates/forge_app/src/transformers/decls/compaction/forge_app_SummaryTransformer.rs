use std :: path :: PathBuf ; use forge_domain :: { ContextSummary , Role , Transformer } ; use crate :: transformers :: dedupe_role :: DedupeRole ; use crate :: transformers :: drop_role :: DropRole ; use crate :: transformers :: strip_working_dir :: StripWorkingDir ; use crate :: transformers :: trim_context_summary :: TrimContextSummary ; # [doc = " Composes all compaction transformers into a single transformation pipeline."] # [doc = ""] # [doc = " This transformer applies a series of transformations to reduce context size"] # [doc = " and improve context quality:"] # [doc = ""] # [doc = " 1. Drops all System role messages"] # [doc = " 2. Deduplicates consecutive User role messages"] # [doc = " 3. Trims context by keeping only the last operation per file path"] # [doc = " 4. Deduplicates consecutive Assistant content blocks"] # [doc = " 5. Strips working directory prefix from file paths"] # [doc = ""] # [doc = " The transformations are applied in sequence using the pipe combinator."] pub struct SummaryTransformer { working_dir : PathBuf , }