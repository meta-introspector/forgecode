use async_trait :: async_trait ; use forge_domain :: { Agent , Conversation , Environment , EventData , EventHandle , ResponsePayload } ; use tracing :: { debug , info } ; use crate :: compact :: Compactor ; # [doc = " Hook handler that performs context compaction when needed"] # [doc = ""] # [doc = " This handler checks if the conversation context has grown too large"] # [doc = " and compacts it according to the agent's compaction configuration."] # [doc = " The handler mutates the conversation's context in-place if compaction"] # [doc = " is triggered."] # [derive (Clone)] pub struct CompactionHandler { agent : Agent , environment : Environment , }