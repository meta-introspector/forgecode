use std :: path :: { Path , PathBuf } ; use std :: time :: Duration ; use bytes :: Bytes ; use derive_setters :: Setters ; use forge_domain :: { AgentId , AnyProvider , Attachment , AuthContextRequest , AuthContextResponse , AuthMethod , ChatCompletionMessage , CommandOutput , Context , Conversation , ConversationId , File , FileInfo , FileStatus , Image , McpConfig , McpServers , Model , ModelId , Node , Provider , ProviderId , ResultStream , Scope , SearchParams , SyncProgress , SyntaxError , Template , ToolCallFull , ToolOutput , WorkspaceAuth , WorkspaceId , WorkspaceInfo , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use url :: Url ; use crate :: user :: { User , UserUsage } ; use crate :: { EnvironmentInfra , Walker } ; # [async_trait :: async_trait] pub trait McpConfigManager : Send + Sync { # [doc = " Responsible to load the MCP servers from all configuration files."] # [doc = " If scope is provided, only loads from that specific scope (not merged)."] async fn read_mcp_config (& self , scope : Option < & Scope >) -> anyhow :: Result < McpConfig > ; # [doc = " Responsible for writing the McpConfig on disk."] async fn write_mcp_config (& self , config : & McpConfig , scope : & Scope) -> anyhow :: Result < () > ; # [doc = " Returns the trusted subset of MCP servers, prompting interactively for"] # [doc = " any project-local config file not yet approved. Must be called once at"] # [doc = " the startup boundary, never on pure config-read paths."] async fn filter_trusted (& self , raw : McpConfig) -> anyhow :: Result < McpConfig > ; }