use std :: path :: { Path , PathBuf } ; use std :: time :: Duration ; use bytes :: Bytes ; use derive_setters :: Setters ; use forge_domain :: { AgentId , AnyProvider , Attachment , AuthContextRequest , AuthContextResponse , AuthMethod , ChatCompletionMessage , CommandOutput , Context , Conversation , ConversationId , File , FileInfo , FileStatus , Image , McpConfig , McpServers , Model , ModelId , Node , Provider , ProviderId , ResultStream , Scope , SearchParams , SyncProgress , SyntaxError , Template , ToolCallFull , ToolOutput , WorkspaceAuth , WorkspaceId , WorkspaceInfo , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use url :: Url ; use crate :: user :: { User , UserUsage } ; use crate :: { EnvironmentInfra , Walker } ; # [doc = " Manages user preferences for default providers and models."] # [async_trait :: async_trait] pub trait AppConfigService : Send + Sync { # [doc = " Gets the current session configuration (provider and model pair)."] # [doc = ""] # [doc = " Returns `None` when no session has been configured yet."] async fn get_session_config (& self) -> Option < forge_domain :: ModelConfig > ; # [doc = " Gets the commit configuration (provider and model for commit message"] # [doc = " generation)."] async fn get_commit_config (& self) -> anyhow :: Result < Option < forge_domain :: ModelConfig > > ; # [doc = " Gets the suggest configuration (provider and model for command"] # [doc = " suggestion generation)."] async fn get_suggest_config (& self) -> anyhow :: Result < Option < forge_domain :: ModelConfig > > ; # [doc = " Gets the current reasoning effort setting."] async fn get_reasoning_effort (& self) -> anyhow :: Result < Option < forge_domain :: Effort > > ; # [doc = " Applies one or more configuration mutations atomically."] # [doc = ""] # [doc = " Each operation in `ops` is applied in order, and the result is"] # [doc = " persisted as a single atomic write. This is the sole write path for"] # [doc = " all configuration changes; use [`forge_domain::ConfigOperation`]"] # [doc = " variants to describe each mutation."] async fn update_config (& self , ops : Vec < forge_domain :: ConfigOperation >) -> anyhow :: Result < () > ; }