use std :: sync :: Arc ; use forge_domain :: { TerminalCommand , TerminalContext } ; use crate :: EnvironmentInfra ; use std :: collections :: BTreeMap ; use std :: sync :: Arc ; use forge_domain :: { Environment , TerminalCommand , TerminalContext } ; use pretty_assertions :: assert_eq ; use super :: * ; use fake :: { Fake , Faker } ; # [doc = " The separator used to join and split environment variable lists."] # [doc = ""] # [doc = " ASCII Unit Separator (`\\x1F`) is chosen because it cannot appear in"] # [doc = " shell command strings, paths, URLs, or exit codes — unlike `:` which"] # [doc = " is common in all of those."] pub const ENV_LIST_SEPARATOR : char = '\x1F' ;