use std :: collections :: { HashMap , HashSet } ; use forge_domain :: { Agent , ToolDefinition , ToolName } ; use glob :: Pattern ; use forge_domain :: { Agent , AgentId , ModelId , ProviderId , ToolDefinition , ToolName } ; use pretty_assertions :: assert_eq ; use super :: ToolResolver ; # [doc = " Service that resolves tool definitions for agents based on their configured"] # [doc = " tool list"] pub struct ToolResolver { all_tool_definitions : Vec < ToolDefinition > , }