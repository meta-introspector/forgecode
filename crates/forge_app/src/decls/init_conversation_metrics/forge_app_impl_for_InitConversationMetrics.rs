use chrono :: { DateTime , Local , Utc } ; use forge_domain :: Conversation ; use forge_domain :: ConversationId ; use super :: * ; impl InitConversationMetrics { pub const fn new (current_time : DateTime < Local >) -> Self { Self { current_time } } pub fn apply (self , mut conversation : Conversation) -> Conversation { conversation . metrics . started_at = Some (self . current_time . with_timezone (& Utc)) ; conversation } }