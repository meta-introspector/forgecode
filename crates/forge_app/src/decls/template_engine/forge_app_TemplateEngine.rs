use std :: sync :: LazyLock ; use forge_domain :: Template ; use handlebars :: { Handlebars , no_escape } ; use include_dir :: { Dir , include_dir } ; use serde :: Serialize ; use serde_json :: json ; use super :: * ; # [doc = " A wrapper around the Handlebars template engine providing a simplified API."] # [doc = ""] # [doc = " This struct provides a clean interface for template rendering using the"] # [doc = " `Template` type from the domain layer."] pub struct TemplateEngine < 'a > { handlebar : Handlebars < 'a > , }