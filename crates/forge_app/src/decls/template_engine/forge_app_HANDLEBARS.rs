use std :: sync :: LazyLock ; use forge_domain :: Template ; use handlebars :: { Handlebars , no_escape } ; use include_dir :: { Dir , include_dir } ; use serde :: Serialize ; use serde_json :: json ; use super :: * ; # [doc = " Global template engine instance with all custom helpers and templates"] # [doc = " registered."] # [doc = ""] # [doc = " This static instance is lazily initialized on first access and provides:"] # [doc = " - The 'inc' helper for incrementing values (useful for 1-based indexing)"] # [doc = " - The 'json' helper for serializing values to JSON strings"] # [doc = " - The 'contains' helper for checking if an array contains a value"] # [doc = " - Strict mode enabled"] # [doc = " - No HTML escaping"] # [doc = " - All embedded templates registered"] # [doc = ""] # [doc = " Use this instance for template rendering throughout the application to avoid"] # [doc = " creating multiple Handlebars instances."] static HANDLEBARS : LazyLock < Handlebars < 'static > > = LazyLock :: new (create_handlebar) ;