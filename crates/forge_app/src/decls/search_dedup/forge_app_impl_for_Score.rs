use std :: cmp :: Ordering ; use std :: collections :: HashMap ; use forge_domain :: { Node , NodeId } ; use forge_domain :: { Node , NodeData } ; use pretty_assertions :: assert_eq ; use super :: * ; impl Score { # [doc = " Creates a new `BestScore` from a query index and search result."] fn new (query_idx : usize , result : & Node) -> Self { Self { query_idx , relevance : result . relevance , distance : result . distance , } } }