use std :: cmp :: Ordering ; use std :: collections :: HashMap ; use forge_domain :: { Node , NodeId } ; use forge_domain :: { Node , NodeData } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Tracks the best score for a node across multiple queries."] # [doc = ""] # [doc = " Implements `Ord` to enable comparison based on score quality."] # [doc = " Priority: relevance (higher is better) → distance (lower is better) →"] # [doc = " similarity (higher is better) → query index (lower is better, tie-breaker)."] # [derive (Debug , Clone , PartialEq)] struct Score { query_idx : usize , relevance : Option < f32 > , distance : Option < f32 > , }