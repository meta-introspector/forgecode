use std :: collections :: { BTreeSet , HashMap } ; use std :: path :: { Path , PathBuf } ; use forge_domain :: { FileHash , FileStatus , SyncProgress , SyncStatus } ; use pretty_assertions :: assert_eq ; use super :: * ; impl SyncProgressCounter { pub fn new (total_files : usize , total_operations : usize) -> Self { Self { total_files , total_operations , completed_operation : 0 } } pub fn complete (& mut self , count : usize) { self . completed_operation += count ; } pub fn sync_progress (& self) -> SyncProgress { if self . completed_operation >= self . total_operations { SyncProgress :: Syncing { current : self . total_files , total : self . total_files } } else { let current : f64 = (self . completed_operation as f64 / self . total_operations as f64) * self . total_files as f64 ; SyncProgress :: Syncing { current : current . floor () as usize , total : self . total_files } } } }