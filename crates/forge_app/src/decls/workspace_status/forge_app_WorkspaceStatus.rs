use std :: collections :: { BTreeSet , HashMap } ; use std :: path :: { Path , PathBuf } ; use forge_domain :: { FileHash , FileStatus , SyncProgress , SyncStatus } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " Result of comparing local and server files"] # [doc = ""] # [doc = " This struct stores remote file information and provides methods"] # [doc = " to compute synchronization operations on-demand. It can derive file statuses"] # [doc = " and identify which files need to be uploaded, deleted, or modified."] # [doc = ""] # [doc = " All paths stored internally are absolute, resolved against the `base_dir`"] # [doc = " provided at construction time."] pub struct WorkspaceStatus { # [doc = " Base directory used to absolutize all paths."] base_dir : PathBuf , # [doc = " Remote file hashes from the server, with absolute paths."] remote_files : Vec < FileHash > , }