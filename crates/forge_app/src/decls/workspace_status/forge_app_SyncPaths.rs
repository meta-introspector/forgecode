use std :: collections :: { BTreeSet , HashMap } ; use std :: path :: { Path , PathBuf } ; use forge_domain :: { FileHash , FileStatus , SyncProgress , SyncStatus } ; use pretty_assertions :: assert_eq ; use super :: * ; # [doc = " The set of file-system operations to perform during a workspace sync."] # [doc = ""] # [doc = " All paths are absolute and resolved against the workspace root."] pub struct SyncPaths { # [doc = " Absolute paths to delete from the remote workspace."] pub delete : Vec < PathBuf > , # [doc = " Absolute local file paths to upload to the remote workspace."] pub upload : Vec < PathBuf > , }