use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Infrastructure trait for providing shared gRPC channel"] # [doc = ""] # [doc = " This trait provides access to a shared gRPC channel for communicating with"] # [doc = " the workspace server. The channel is lazily connected and can be cloned"] # [doc = " cheaply across multiple clients."] pub trait GrpcInfra : Send + Sync { # [doc = " Returns a cloned gRPC channel for the workspace server"] fn channel (& self) -> anyhow :: Result < tonic :: transport :: Channel > ; # [doc = " Hydrates the gRPC channel by establishing and then dropping the"] # [doc = " connection"] fn hydrate (& self) ; }