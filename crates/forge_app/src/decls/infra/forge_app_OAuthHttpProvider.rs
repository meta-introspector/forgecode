use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Provides HTTP features for OAuth authentication flows."] # [async_trait :: async_trait] pub trait OAuthHttpProvider : Send + Sync { # [doc = " Builds an authorization URL with provider-specific parameters."] async fn build_auth_url (& self , config : & OAuthConfig) -> anyhow :: Result < AuthCodeParams > ; # [doc = " Exchanges an authorization code for an access token with"] # [doc = " provider-specific handling."] async fn exchange_code (& self , config : & OAuthConfig , code : & str , verifier : Option < & str > ,) -> anyhow :: Result < OAuthTokenResponse > ; # [doc = " Creates an HTTP client with provider-specific headers and behavior."] fn build_http_client (& self , config : & OAuthConfig) -> anyhow :: Result < reqwest :: Client > ; }