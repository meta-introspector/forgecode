use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Service for reading multiple files from a directory asynchronously"] # [async_trait :: async_trait] pub trait DirectoryReaderInfra : Send + Sync { # [doc = " Lists all entries (files and directories) in a directory without reading"] # [doc = " file contents Returns a vector of tuples containing (entry_path,"] # [doc = " is_directory) This is much more efficient than read_directory_files"] # [doc = " when you only need to list entries"] async fn list_directory_entries (& self , directory : & Path ,) -> anyhow :: Result < Vec < (PathBuf , bool) > > ; # [doc = " Reads all files in a directory that match the given filter pattern"] # [doc = " Returns a vector of tuples containing (file_path, file_content)"] # [doc = " Files are read asynchronously/in parallel for better performance"] async fn read_directory_files (& self , directory : & Path , pattern : Option < & str > ,) -> anyhow :: Result < Vec < (PathBuf , String) > > ; }