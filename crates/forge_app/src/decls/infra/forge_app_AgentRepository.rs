use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Repository for loading agents from multiple sources."] # [doc = ""] # [doc = " This trait provides access to fully-resolved domain [`forge_domain::Agent`]"] # [doc = " values from:"] # [doc = " 1. Built-in agents (embedded in the application)"] # [doc = " 2. Global custom agents (from ~/.forge/agents/ directory)"] # [doc = " 3. Project-local agents (from .forge/agents/ directory in current working"] # [doc = "    directory)"] # [doc = ""] # [doc = " ## Agent Precedence"] # [doc = " When agents have duplicate IDs across different sources, the precedence"] # [doc = " order is: **CWD (project-local) > Global custom > Built-in**"] # [doc = ""] # [doc = " This means project-local agents can override global agents, and both can"] # [doc = " override built-in agents."] # [async_trait :: async_trait] pub trait AgentRepository : Send + Sync { # [doc = " Load all agents from all available sources with conflict resolution."] # [doc = ""] # [doc = " # Arguments"] # [doc = ""] # [doc = " * `provider_id` - Default provider applied to agents that do not specify"] # [doc = "   one"] # [doc = " * `model_id` - Default model applied to agents that do not specify one"] async fn get_agents (& self) -> anyhow :: Result < Vec < forge_domain :: Agent > > ; # [doc = " Load lightweight metadata for all agents without requiring a configured"] # [doc = " provider or model."] async fn get_agent_infos (& self) -> anyhow :: Result < Vec < forge_domain :: AgentInfo > > ; }