use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Repository for accessing system environment information"] # [doc = " This uses the EnvironmentService trait from forge_domain"] # [doc = " A service for reading files from the filesystem."] # [doc = ""] # [doc = " This trait provides an abstraction over file reading operations, allowing"] # [doc = " for both real file system access and test mocking."] # [async_trait :: async_trait] pub trait FileReaderInfra : Send + Sync { # [doc = " Reads the content of a file at the specified path."] # [doc = " Returns the file content as a UTF-8 string."] async fn read_utf8 (& self , path : & Path) -> anyhow :: Result < String > ; # [doc = " Reads multiple files in batches and returns a stream of file results."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `batch_size` - Number of files to read concurrently per batch"] # [doc = " * `paths` - Vector of file paths to read"] # [doc = ""] # [doc = " Returns a stream where each item is a tuple containing (file_path,"] # [doc = " file_content). Files are processed in batches internally for concurrency"] # [doc = " control."] fn read_batch_utf8 (& self , batch_size : usize , paths : Vec < PathBuf > ,) -> impl futures :: Stream < Item = (PathBuf , anyhow :: Result < String >) > + Send ; # [doc = " Reads the content of a file at the specified path."] # [doc = " Returns the file content as raw bytes."] async fn read (& self , path : & Path) -> anyhow :: Result < Vec < u8 > > ; # [doc = " Reads a specific line range from a file at the specified path."] # [doc = " Returns the file content within the range as a UTF-8 string along with"] # [doc = " metadata."] # [doc = ""] # [doc = " - start_line specifies the starting line position (1-based, inclusive)."] # [doc = " - end_line specifies the ending line position (1-based, inclusive)."] # [doc = " - Both start_line and end_line are inclusive bounds."] # [doc = " - Binary files are automatically detected and rejected."] # [doc = ""] # [doc = " Returns a tuple containing the file content and FileInfo with metadata"] # [doc = " about the read operation:"] # [doc = " - FileInfo.start_line: starting line position"] # [doc = " - FileInfo.end_line: ending line position"] # [doc = " - FileInfo.total_lines: total line count in file"] # [doc = " - FileInfo.content_hash: SHA-256 hash of the **full** file content,"] # [doc = "   allowing callers to store a stable hash that matches what a whole-file"] # [doc = "   read produces (used by the external-change detector)"] async fn range_read_utf8 (& self , path : & Path , start_line : u64 , end_line : u64 ,) -> anyhow :: Result < (String , FileInfo) > ; }