use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Infrastructure trait for accessing environment configuration, system"] # [doc = " variables, and persisted application configuration."] pub trait EnvironmentInfra : Send + Sync { # [doc = " The fully-resolved configuration type stored by the implementation."] type Config : Clone + Send + Sync ; fn get_env_var (& self , key : & str) -> Option < String > ; fn get_env_vars (& self) -> BTreeMap < String , String > ; # [doc = " Retrieves the current application configuration as an [`Environment`]."] fn get_environment (& self) -> Environment ; # [doc = " Returns the latest fully-resolved configuration, re-reading from disk"] # [doc = " if a prior `update_environment` call has invalidated the cache."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the disk read fails."] fn get_config (& self) -> anyhow :: Result < Self :: Config > ; # [doc = " Applies a list of configuration operations to the persisted config."] # [doc = ""] # [doc = " Implementations should load the current config, apply each operation in"] # [doc = " order, and persist the result atomically."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the configuration cannot be read or written."] fn update_environment (& self , ops : Vec < ConfigOperation > ,) -> impl std :: future :: Future < Output = anyhow :: Result < () > > + Send ; }