use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [doc = " Generic cache repository for content-addressable storage."] # [doc = ""] # [doc = " This trait provides an abstraction over caching operations with support for"] # [doc = " arbitrary key and value types. Keys must be hashable and serializable, while"] # [doc = " values must be serializable. The trait is designed to work with"] # [doc = " content-addressable storage systems like cacache."] # [doc = ""] # [doc = " All operations return `anyhow::Result` for consistent error handling across"] # [doc = " the infrastructure layer."] # [async_trait :: async_trait] pub trait KVStore : Send + Sync { # [doc = " Retrieves a value from the cache by its key."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `key` - The key to look up in the cache"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the cache operation fails"] async fn cache_get < K , V > (& self , key : & K) -> Result < Option < V > > where K : Hash + Sync , V : serde :: Serialize + DeserializeOwned + Send ; # [doc = " Stores a value in the cache with the given key."] # [doc = ""] # [doc = " If the key already exists, the value is overwritten."] # [doc = " Uses content-addressable storage for integrity verification."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `key` - The key to store the value under"] # [doc = " * `value` - The value to cache"] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the cache operation fails"] async fn cache_set < K , V > (& self , key : & K , value : & V) -> Result < () > where K : Hash + Sync , V : serde :: Serialize + Sync ; # [doc = " Clears all entries from the cache."] # [doc = ""] # [doc = " This operation removes all cached data. Use with caution."] # [doc = ""] # [doc = " # Errors"] # [doc = " Returns an error if the cache clear operation fails"] async fn cache_clear (& self) -> Result < () > ; }