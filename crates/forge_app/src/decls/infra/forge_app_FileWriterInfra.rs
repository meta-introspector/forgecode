use std :: collections :: BTreeMap ; use std :: hash :: Hash ; use std :: path :: { Path , PathBuf } ; use anyhow :: Result ; use bytes :: Bytes ; use forge_domain :: { AuthCodeParams , CommandOutput , ConfigOperation , Environment , FileInfo , McpServerConfig , OAuthConfig , OAuthTokenResponse , ToolDefinition , ToolName , ToolOutput , } ; use forge_eventsource :: EventSource ; use reqwest :: Response ; use reqwest :: header :: HeaderMap ; use serde :: de :: DeserializeOwned ; use url :: Url ; use crate :: { WalkedFile , Walker } ; # [async_trait :: async_trait] pub trait FileWriterInfra : Send + Sync { # [doc = " Writes the content of a file at the specified path."] async fn write (& self , path : & Path , contents : Bytes) -> anyhow :: Result < () > ; # [doc = " Appends content to a file at the specified path, creating it if it does"] # [doc = " not exist."] async fn append (& self , path : & Path , contents : Bytes) -> anyhow :: Result < () > ; # [doc = " Writes content to a temporary file with the given prefix and extension,"] # [doc = " and returns its path. The file will be kept (not deleted) after"] # [doc = " creation."] # [doc = ""] # [doc = " # Arguments"] # [doc = " * `prefix` - Prefix for the temporary file name"] # [doc = " * `ext` - File extension (e.g. \".txt\", \".md\")"] # [doc = " * `content` - Content to write to the file"] async fn write_temp (& self , prefix : & str , ext : & str , content : & str) -> anyhow :: Result < PathBuf > ; }